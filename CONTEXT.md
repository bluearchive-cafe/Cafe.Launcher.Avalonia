# Cafe Launcher — 项目上下文

本文件描述启动器管理游戏安装与运行时使用的核心领域概念，以及设计系统的决策记录。工程结构与工作流见 `AGENTS.md`；编码规范见 `PROJECT_CONVENTIONS.md`；界面文案术语与规范译法见 `UBIQUITOUS_LANGUAGE.md`。

## 领域语言：安装生命周期

**本地安装状态**:
启动器用来判断本地游戏安装身份、版本和受管理文件集合的一组持久化状态。它不包含游戏文件本身。
_Avoid_: 本地游戏状态、下载状态

**游戏安装路径**:
游戏安装目录的规范化位置，是下载、卸载和本地安装状态访问共同使用的路径。规范化后固定为 `YostarGames\BlueArchive_JP`。
_Avoid_: 游戏路径、本地路径

**运行环境配置**:
用户为游戏启动选择的运行器及其可选自定义路径、兼容前缀和 Proton 路径；自动选择表示不固定具体运行器。
_Avoid_: 运行器选项、运行时参数

**启动目标**:
从本地安装状态解析并经磁盘验证的游戏启动目标，包含可执行文件名、完整路径、工作目录与启动参数；远端配置不参与解析，启动与快捷方式共用同一结果。
_Avoid_: 快捷方式目标、启动路径、远端启动名

**游戏进程家族**:
该游戏自己的那一组可执行文件名及其同族变体，由游戏带来的启动配置（宿主名与启动参数里的可执行文件）推导；尚未安装（还没有本地配置）时退回远端配置声明的启动程序名。它是「游戏是不是在跑」这一判定的判据：计划/预检阶段问一次，并在真正动文件之前再问一次——卸载的删除边界、下载与修复的写入边界，因为确认框和数分钟的下载都可能比第一次答复活得更久。按名字识别而不是按镜像路径，因为反作弊会保护进程对象、读不到路径。
_Avoid_: 宿主进程、游戏进程、运行中的进程

**未安装**:
游戏安装路径中不存在任何本地安装状态。

**有效安装状态**:
本地安装状态完整存在，且其中的身份、版本和受管理文件信息全部有效。

**损坏安装状态**:
本地安装状态不完整，或其中任一部分无法通过格式与完整性校验。
_Avoid_: 部分安装状态

**安装状态读取失败**:
由于 I/O 或权限问题，启动器无法确定本地安装状态。

**需要强制更新**:
本地游戏版本低于远端允许启动的最低版本。

**有可用更新**:
本地游戏版本不低于远端允许启动的最低版本，但低于远端最新版本。

**可启动**:
本地安装状态有效，且本地游戏版本不低于远端最新版本。

**远端状态不可用**:
本地安装状态有效，但启动器无法取得判断版本兼容性所需的远端游戏配置。

## 诊断与故障

**不可恢复崩溃**:
未处理异常或被明确判定为无法安全继续的严重故障；进程必须终止。它不包含取消操作、网络失败、配置校验失败等已有恢复路径的业务错误。
_Avoid_: 普通错误、可恢复严重错误

**可恢复严重错误**:
虽需突出告知用户，但启动器仍处于可信状态、允许用户确认后继续操作的故障。它沿用现有严重错误遮罩，不触发崩溃退出流程。
_Avoid_: 不可恢复崩溃、FatalCrash

**崩溃信息窗口**:
不可恢复崩溃发生后独立于主窗口显示的故障说明界面；默认提供友好说明，将技术细节置于可展开区域，并且不允许用户继续运行已失去可信状态的进程。
_Avoid_: 错误 Toast、主窗口错误遮罩、继续提示框

**崩溃快照**:
为一次不可恢复崩溃同步保存的诊断记录，是隔离报告模式和后续排查共同读取的事实来源；同一次崩溃只有一份主快照。
_Avoid_: 普通运行日志、错误 Toast 内容

## 领域语言：设置编辑

**设置草稿**:
设置编辑器持有、会被下一次保存写入的当前值；用户对设置项的改动直接落在它上面。
_Avoid_: 当前设置、临时设置

**设置快照**:
编辑器持有的已保存设置的副本，是判定「有没有改动」与「放弃改动回到哪」的共同参照。
_Avoid_: 原始设置、原始值

**已保存设置**:
`settings.json` 里当前生效的那份设置，磁盘与设置快照都是它的表示——它只有一个值，两者必须一起推进。生产代码对它的每一次改写都经唯一写入方（`ISavedSettingsWriter`）：调用方只描述改什么，由写入方落盘、再把落盘的归一化值就地写回编辑器。资源面板 UID、窗口几何、游戏路径这些「由别的模块顺手改一个字段」的路径尤其要走这里，否则它们的字段会在用户下一次保存设置时被写回旧值。
_Avoid_: 磁盘设置、落盘设置

**数据根**:
启动器私有数据的根目录（生产为 `%LOCALAPPDATA%\Cafe Launcher`，测试为临时目录），以及根内知名路径的唯一出处（`LauncherDataRoot`）。进程根只在少数几处解析——组合根一次、ADR-019 保护的 pre-DI 路径各一次——其余模块接收注入的实例，因此「数据放哪」不是一个谁都读得到的进程级静态。测试换根只经 `TestDataRoot` 一个接缝。
_Avoid_: 用户数据目录、应用数据目录

**客户端偏好**:
池化 HTTP 客户端的连接级策略（HTTP/2 开关、代理模式）。两者都按使用时机从已保存设置里读出来解析——`HttpClientFactory` 与远程传输各由组合根注入一个读已保存快照的闭包——所以没有任何调用方需要「把偏好推给工厂」（[ADR-028](docs/design/adr/ADR-028-客户端偏好按使用时机拉取.md)）。
_Avoid_: 客户端配置推送、工厂开关

**操作拒绝**:
判定「当前安装状态不允许该操作」的结果（`GameOperationDecision.RejectedForCurrentState`）。策略表只回答允不允许；配哪句话、以警告还是失败结果呈现，是调用层的事——但用户已在确认框上点过确认之后仍被拒绝时，必须给出可见反馈（[ADR-027](docs/design/adr/ADR-027-策略判定结果与确认后拒绝反馈.md)）。
_Avoid_: 权限失败、操作不可用标记

**预检失败**:
执行层校验（`ValidateAsync`）返回的类型化失败，与**操作拒绝**是两道闸：策略答「当前状态允不允许」，预检答「这个路径真的能删吗」。用户手势触发的预检失败同样不允许静默——`ValidateUninstallAsync` 报出执行层给出的具体原因后返回 `null`，调用方不得把 `null` 当成「无事发生」（[ADR-029](docs/design/adr/ADR-029-卸载预检失败不再静默.md)）。
_Avoid_: 校验错误、前置检查不通过

**彻底清除**:
卸载范围（`UninstallScope.ThoroughCleanup`）：除标准卸载删的清单内文件、两个状态文件与桌面快捷方式外，再删整棵安装目录与受管兼容子树 `compatibility/<gameId>`。用户自定义到别处的 Prefix 属于用户自己的目录，保留并说明；确认框里给出的目录大小与实际删除目标同源（[ADR-030](docs/design/adr/ADR-030-卸载可彻底清除.md)）。
_Avoid_: 深度卸载、强制删除

**停止意图**:
调用方对「为什么停止活动工作流」的表态（`GameOperationStopIntent`：用户停止／进程退出）。它是域外唯一可用的说法，检查点去留那套策略词表留在游戏操作域内，由域翻译一次（[ADR-026](docs/design/adr/ADR-026-停止意图由游戏操作域翻译.md)）。注意「关窗并停止」是用户停止，不是进程退出。
_Avoid_: 停止原因、下载停止码

**状态同一性**:
设置草稿与设置快照等价的判定；它是设置页保存按钮启用态的唯一依据，由设置模型自身定义（`LauncherSettings.HasSameSettingsState`）而非在消费方逐项维护。
_Avoid_: 脏检查、相等比较

**启动后行为**:
游戏成功启动后启动器的动作（`AfterLaunchBehaviors`：保持窗口／最小化到托盘／退出启动器）。规则由游戏操作旅程按快照里的设置翻译一次，呈现层只收到两个单用途窗口动词之一。其中的「退出启动器」刻意不经由关闭路径——那条路会把关闭意图交给关闭行为解释，用户在关闭行为里选的是「最小化到托盘」时，本设置会退化成静默的托盘隐藏（[ADR-031](docs/design/adr/ADR-031-启动后行为可配置且退出不走关闭路径.md)）。
_Avoid_: 启动完成动作、启动后处理

---

# 设计系统（P2 决策会话）

> 本段与 `docs/design/design-system-spec.md`（Q1–Q24 决策册）互为引用；P1（M0–M5）已全部完成并提交。
> P2 待定决策在此逐轮定案；每项定案后追加 ADR 至 `docs/design/adr/`。偏好建议方 = 代理，最终选择权 = 用户。

## 动效语言

**Fluent 动效层**:
覆盖整个启动器的行为型动效规范；采用 Windows Fluent 的连续、一致、响应式原则，但不替换既有 M3 色彩、形状、组件与布局语义。
_Avoid_: Fluent 全面重设计、M3 动效

**完整动效**:
由用户明确选择、始终启用完整产品动效的模式；即使 Windows 已关闭动画，该模式仍以用户在启动器内的选择为准。
_Avoid_: 系统动效

**系统动效**:
跟随 Windows 动画开关决定是否启用完整产品动效的模式。
_Avoid_: 自动员效

**降动效**:
移除所有空间移动、自动轮播与装饰性过渡，同时允许临时表面使用极短的纯透明度变化，以保留状态可理解性和即时操作反馈。
_Avoid_: 无反馈、禁用界面更新

**即时反馈**:
紧随指针、键盘或触控输入发生的轻量状态响应，以颜色、状态层、描边或指示器表达，不使用缩放或弹跳。
_Avoid_: 微动画

**内容切换**:
同一容器内不同内容之间的替换；没有空间方向时以淡化维持稳定，有明确前后关系时归入空间连续。
_Avoid_: 页面动画

**空间连续**:
通过与导航方向一致的短距离移动，说明用户主动触发的前进、后退或同级浏览关系；自动轮播不使用方向移动。
_Avoid_: 滑屏特效

**临时表面**:
覆盖当前任务出现的对话框、设置层、工具层或其他模态表面；遮罩淡化，表面只进行一次短距离进入或退出，内部内容不重复移动。
_Avoid_: 弹窗动画

**重要完成**:
安装、更新、修复、卸载或首次设置向导成功完成时出现的任务结果；其强调高于日常反馈，但仍须短促、可中断，且不能成为状态的唯一表达。未改变安装生命周期的检查结果、设置保存和路径更新等日常成功不属于重要完成。
_Avoid_: 庆祝动画

**游戏操作表面**:
主界面底部承载安装、控制和进度状态的同一任务容器；状态切换时保持底边锚定，通过容器连续变化表达任务延续。
_Avoid_: 安装面板、控制面板、进度面板

**壳层入场**:
主窗口首次显示时，窗口内部完整内容作为一个整体进行的一次快速淡入；不包含标题栏、轮播或操作表面的依次入场。
_Avoid_: 启动画面、开屏动画

**任务完成转换**:
安装、更新、修复或卸载完成后，游戏操作表面原位转换为成功状态；首次设置向导则在当前向导表面显示短确认态。两者都不使用弹跳、撒彩纸或覆盖界面的庆祝效果。
_Avoid_: 完成弹窗、庆祝动画

**外观预览切换**:
设置页即时预览外观变更的呈现方式；壁纸作为单一视觉对象交叉淡化，主题色与配色 Token 同步切换而不逐项插值。
_Avoid_: 主题动画

**任务中断结果**:
游戏操作被用户停止或因错误失败后的可恢复状态；它保留任务上下文并提供警告或错误反馈，但不使用任务完成转换。
_Avoid_: 完成、取消成功

**模态交互权**:
模态栈中只有顶层表面能够接收输入；顶层关闭时，交互权和焦点须在退出完成后才归还下层表面或触发者。
_Avoid_: 遮罩点击穿透

实现分工（2026-09-12 审计裁定）：七个主叠层（设置、资源面板、日志查看器、日志导出、调试面板、设计画廊、设置向导）各自在叠层根上绑定一个按种类区分的 `ModalHostViewModel.Is*Interactive`；**对话框层有意不设此类闸口**——它的可见性由 `DialogsViewModel.Is*Visible` 驱动而非 `ModalKind` 属性，再加一道闸口会形成第二个真相来源，使「已显示但未注册进模态栈」的对话框渲染出来却不可交互（窗口硬冻结），故该层的输入拦截由全屏遮罩与 `ZIndex` 次序承担。详见 `AGENTS.md` 的模态隔离条款。

## 已定骨架（P1 落地，勿再开）

- **M3 语义 / FluentTheme 底座**；点分层 token（`Launcher.*` 十二家族）；AA 契约自动化；动态色管线（ApplyScheme）；黄金截图基线；Debug 门画廊。
- **组件四型映射**：`primary-action`→filled、`flat-action`→outlined（语境可 tonal）、`text-link`→text、`danger-action`→error-filled；共享模板 `LauncherBorderButtonTemplate` 保留；诊断/日志例外（Fluent 基础模板，仅 token 兼容）。
- **表面与 Z 序**：单窗口 + 覆盖层（100/200/500/1000 不动，常量在 `LauncherConstants`）；P3 表面顺序 = 主壳→设置→对话框+Toast→向导→诊断/日志/资源面板（Q22）。

## P2 决策前沿

### Round 1（2026-08-25）

1. **底栏仲裁标准** → [ADR-001](docs/design/adr/ADR-001-底栏仲裁标准.md)：四标准等权 + 可读性一票否决；**形态本身待原型分支**。
2. **圆角字阶一次性迁移** → [ADR-002](docs/design/adr/ADR-002-圆角字阶迁移.md)：Xs=4/Sm=8/Md=12/Lg=16，全量消费同步 + 基线重生成。
3. **控件高度保持现值** → [ADR-003](docs/design/adr/ADR-003-控件高度保持.md)：P3 再评。
4. **按钮四型规格** → [ADR-004](docs/design/adr/ADR-004-按钮四型规格.md)：「值不动、语言统一」+ 模板参数化。
5. **字阶数值保持** → [ADR-005](docs/design/adr/ADR-005-字阶数值保持.md)：仅角色键消费迁移。
6. **设置页布局范围** → [ADR-006](docs/design/adr/ADR-006-设置页布局范围.md)：仅组件规格统一，骨架不动。
7. **画廊状态矩阵范围** → [ADR-007](docs/design/adr/ADR-007-画廊矩阵范围.md)：3 组件 × 6 态。

### Round 2 + 设置页方向（2026-08-25）

8. **组件批次映射** → [ADR-008](docs/design/adr/ADR-008-组件批次映射.md)：A=模板+四型按钮，B=现存组件对齐，C=新组件+矩阵+清单。
9. **设置三项 UI + 算法落地** → [ADR-009](docs/design/adr/ADR-009-设置三项UI与算法落地.md)：3×SettingRow(ComboBox)，次序算法→变体→中性；P2 实现 Celebi+Score 提取器接入。
10. **中性色切换交互** → [ADR-010](docs/design/adr/ADR-010-中性色切换交互.md)：一行 ComboBox + caption 提示，无阻断。
11. **字阶迁移时机** → [ADR-011](docs/design/adr/ADR-011-字阶迁移时机.md)：随组件批次。
12. **底栏原型启动** → [ADR-012](docs/design/adr/ADR-012-底栏原型启动.md)：**已撤销**（2026-08-25 用户放弃首页布局决策；Q18 形态重新开放，仲裁标准 ADR-001 保留）。
13. **设置页重设计方向** → [ADR-013](docs/design/adr/ADR-013-设置页重设计方向.md)：**变体 B（纯列表 + hairline 分隔线）**；导航选中态 = SecondaryContainer 底 + OnSecondaryContainer 字 + leading icon（无指示条）；设置页蓝图已定稿（原型见 `prototype/settings-page`）。
14. **其余界面重设计方向** → [ADR-014](docs/design/adr/ADR-014-其余表面M3重设计方向.md)：对话框/Toast/设置向导；**主壳首页仍搁置**；资源面板/日志/调试仅 token 兼容（Q3）。

### Fluent 动效重设计（2026-08-27）

- **M3 视觉之上的 Fluent 动效层** → [ADR-016](docs/design/adr/ADR-016-Fluent动效层.md)：五类动效语义、Windows 时长与曲线、完整/系统/降动效边界、响应优先降级，以及独立 Avalonia 原型门禁。

### 执行状态

- **P2 执行 ticket 拆分已执行完毕（2026-08-26）**——A→B→C 批次 + 设置三项 UI（`SettingSelect`）+ Celebi+Score 提取器接入 + 中性色 caption（ADR-010）+ 走查清单（`docs/design/design-walkthrough-checklist.md`）全部落地。
- P3 目前完成 = 设置页（ADR-013）+ 对话框/Toast/设置向导（ADR-014/017）+ 关于分区（ADR-018）+ 崩溃窗口（ADR-019/020）。
- P2 剩余项：底栏双原型对比区随 Q18 重新裁决；画廊矩阵渲染细节随批次微调。

### 🔓 仍开放

- **底栏形态（Q18）**：仲裁结论已撤销，重新开放（ADR-001 标准保留；`prototype/bottom-bar` 素材保留）。
- **首页（主壳）重设计**：已搁置（2026-08-25 用户放弃本轮设计稿与相关布局决策）；P3 主壳表面照旧包含，但无既定蓝图。

## ADR 索引

| ADR | 决策 | 状态 |
|---|---|---|
| ADR-001 | 底栏仲裁标准（四标准 + 一票否决） | ✅ 已接受 |
| ADR-002 | 圆角字阶一次性迁移（4/8/12/16） | ✅ 已接受 |
| ADR-003 | 控件高度保持现值 | ✅ 已接受 |
| ADR-004 | 按钮四型规格（值不动、语言统一） | ✅ 已接受 |
| ADR-005 | 字阶数值保持现状 | ✅ 已接受 |
| ADR-006 | 设置页布局自由度下限 | ✅ 已接受 |
| ADR-007 | 画廊状态矩阵范围（3×6） | ✅ 已接受 |
| ADR-008 | 组件批次 A→B→C 映射 | ✅ 已接受 |
| ADR-009 | 设置三项 UI + Celebi+Score 落地 | ✅ 已接受 |
| ADR-010 | 中性色切换交互 | ✅ 已接受 |
| ADR-011 | 字阶迁移随批次 | ✅ 已接受 |
| ADR-012 | 底栏双原型启动（Q18 仲裁结论已撤销，形态重新开放） | ⛔ 已撤销 |
| ADR-013 | 设置页重设计方向（变体 B + SecondaryContainer 导航选中） | ✅ 已接受 |
| ADR-014 | 对话框/Toast/设置向导 M3 重设计方向（主壳仍搁置） | ✅ 已接受 |
| ADR-015 | 对话框家族 v2（DialogSurface 统一框架） | ✅ 已接受 |
| ADR-016 | M3 视觉之上的 Fluent 动效层 | ✅ 已接受 |
| ADR-017 | 设置向导动效落地与 M3 界面细化 | ✅ 已接受 |
| ADR-018 | 关于分区重设计方向 | ✅ 已接受 |
| ADR-019 | 不可恢复崩溃两级兜底 | ✅ 已接受 |
| ADR-020 | 崩溃窗口独立 Fluent 设计系统 | ✅ 已接受 |
| ADR-021 | 保留操作后端接缝（否决删除 GameOperationExecutor 直通带） | ✅ 已接受 |
| ADR-022 | DownloadSession 内部接缝维持私有分段（否决升格 Plan/Execute 为独立类） | ✅ 已接受 |
| ADR-023 | 模态栈注册收敛为声明式注册记录（修订 ADR-015 显示管线冻结条款） | ✅ 已接受 |
| ADR-024 | 已保存设置只有一个写入方（`ISavedSettingsWriter`） | ✅ 已接受 |
| ADR-025 | 数据根由组合根解析一次并注入（`LauncherDataRoot`） | ✅ 已接受 |
| ADR-026 | 停止意图由游戏操作域翻译，域外不命名检查点决策 | ✅ 已接受 |
| ADR-027 | 策略给判定结果，确认之后的拒绝必须有可见反馈 | ✅ 已接受 |
| ADR-028 | 客户端偏好按使用时机拉取，工厂不接受偏好推送 | ✅ 已接受 |
| ADR-029 | 卸载预检失败报出执行层原因，不静默返回 | ✅ 已接受 |
| ADR-030 | 卸载可彻底清除，标准卸载也删快捷方式并回报结果 | ✅ 已接受 |
| ADR-031 | 启动后行为可配置，启动退出不走关闭路径 | ✅ 已接受 |
| ADR-032 | 游戏进程按名字家族识别，卸载等文件操作只在整族退出后放行 | ✅ 已接受 |
