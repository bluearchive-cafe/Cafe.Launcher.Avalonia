<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="using:Cafe.Launcher.Avalonia.Views"
        xmlns:helpers="using:Cafe.Launcher.Avalonia.Helpers"
        xmlns:controls="using:Cafe.Launcher.Avalonia.Controls"
        xmlns:materialIcons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia">
    <Styles.Resources>
        <ControlTemplate x:Key="LauncherBorderButtonTemplate" TargetType="Button">
            <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{TemplateBinding CornerRadius}">
                <ContentPresenter Content="{TemplateBinding Content}"
                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                  Padding="{TemplateBinding Padding}"
                                  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
        </ControlTemplate>
    </Styles.Resources>
    <Style Selector="TextBlock.caption">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Sm}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <Style Selector="TextBlock.value">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="TextBlock.heading">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Display}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
    </Style>
    <Style Selector="TextBlock.body">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Body}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style Selector="TextBlock.dialog-title">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Headline.Lg}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
    </Style>
    <Style Selector="TextBlock.kicker">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Label.Sm}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="TextBlock.titlebar-brand">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Title.Md}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
    </Style>
    <Style Selector="TextBlock.titlebar-status">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome.Muted}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Label.Md}"/>
    </Style>
    <Style Selector="TextBlock.progress-title">
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Lg}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
    </Style>
    <Style Selector="TextBlock.on-dark-caption">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnDark}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
    </Style>
    <Style Selector="StackPanel.titlebar-brand-row">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.TitleBar.Brand.Margin}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="StackPanel.titlebar-actions">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.TitleBar.Actions.Margin}"/>
    </Style>
    <Style Selector="Button.chrome">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.chrome.settings">
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.TitleBar.Settings.Margin}"/>
    </Style>
    <Style Selector="Button.chrome:pointerover">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Chrome.Hover}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
    </Style>
    <Style Selector="Button.chrome:pressed">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Chrome.Pressed}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
    </Style>
    <Style Selector="Button.chrome.close:pointerover">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Danger}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
    </Style>
    <Style Selector="Button.chrome.close:pressed">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Danger.Pressed}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
    </Style>
    <Style Selector="Button:focus-visible">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="ComboBox:focus-visible">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="ComboBox.setting-control">
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Component.Settings.Control.MinWidth}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Field.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Field.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Select.Padding}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Field}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="ComboBox.setting-control:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="ComboBox.setting-control:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="ComboBox.setting-control:focus-visible">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="ComboBox.setting-control:disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="ToggleSwitch:focus-visible">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="Button.text-link">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Link}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.text-link:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.text-link:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Button.text-link:disabled">
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="HyperlinkButton.inline-legal-link">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Link}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Control.Size.None}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Size.None}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Sm}"/>
        <Setter Property="TextBlock.TextDecorations" Value="Underline"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="HyperlinkButton.inline-legal-link:pointerover">
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="HyperlinkButton.inline-legal-link:pressed">
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="HyperlinkButton.inline-legal-link:disabled">
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="HyperlinkButton.inline-legal-link:focus-visible">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="Button.icon-link">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Compact.Padding}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.icon-link:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Info.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.icon-link:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Button.icon-link:disabled">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.icon-button">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Size.Swatch}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Size.Swatch}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.icon-button.carousel-navigation">
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
    </Style>
    <Style Selector="Button.icon-button:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.icon-button:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Button.icon-button:disabled">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <!-- carousel 箭头压在壁纸上，状态反馈走 chrome 态（与 banner-link 同配方）；
         图标前景保持 OnChrome 豁免（spec §8），反馈由背景状态层承担。 -->
    <Style Selector="Button.icon-button.carousel-navigation:pointerover">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Chrome.Hover}"/>
    </Style>
    <Style Selector="Button.icon-button.carousel-navigation:pressed">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Chrome.Pressed}"/>
    </Style>
    <Style Selector="Button.primary-action">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Title.Md}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Component.PrimaryAction.MinWidth}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Bottom}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Primary.Padding}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.primary-action:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
    </Style>
    <Style Selector="Button.primary-action:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
    </Style>
    <Style Selector="Button.primary-action:disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <!-- 紧凑 filled 变体（ADR-018 关于分区）：与 flat-action 同密度，供分区内唯一最高强调操作。 -->
    <Style Selector="Button.primary-action.compact-action">
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Control.MinWidth.None}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Outlined.Padding}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Sm}"/>
    </Style>
    <Style Selector="Button.flat-action">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Button.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Outlined.Padding}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.flat-action:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.flat-action:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.flat-action:disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
    </Style>
    <Style Selector="Button.flat-action.icon-only">
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="Button.bottom-action">
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Bottom}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Component.BottomAction.MinWidth}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Primary.Padding}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Lg}"/>
    </Style>
    <Style Selector="StackPanel.confirm-actions">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="Spacing" Value="{StaticResource Launcher.Spacing.Md}"/>
    </Style>
    <Style Selector="Button.danger-action">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Dialog}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Primary.Padding}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.flat-action.danger-action">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Error}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Outlined.Padding}"/>
    </Style>
    <Style Selector="Button.danger-action:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.danger-action:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.danger-action:disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <Style Selector="Button.dialog-action">
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Dialog}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Component.Dialog.Action.MinWidth}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Dialog.Padding}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Component.Dialog.BasicAction.MinWidth}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Dialog.BasicAction.Padding}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Xs}"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action.primary-action">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action.primary-action:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action.primary-action:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action.danger-action">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action.danger-action:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action.danger-action:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action:disabled">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <Style Selector="Button.primary-action.dialog-action:disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <Style Selector="Button.confirm-dialog-action.danger-action:disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <Style Selector="Button.dialog-close">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.dialog-close:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Dialog.Close.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="Button.dialog-close:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Dialog.Close.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="Button.dialog-close:disabled">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.dialog-close:focus-visible">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Soft}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
    </Style>
    <Style Selector="Button.dialog-close.header-action">
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.Dialog.HeaderAction.Margin}"/>
    </Style>
    <Style Selector="Button.launcher-control">
        <Setter Property="Width" Value="{StaticResource Launcher.Component.LauncherControl.Width}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Launch}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Launch}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Action.Launcher.Padding}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Title.Lg}"/>
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.launcher-control.start">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Launch}"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Button.launcher-control.start:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Button.launcher-control.start:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Button.launcher-control.manage">
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Launch}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SiteButton.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.SiteButton.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Button.launcher-control.manage:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SiteButton.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Button.launcher-control.manage:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Info.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Button.launcher-control:disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="TextBlock.operation-status-title">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Title.Lg}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="TextBlock.operation-status-title.on-dark-title">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
    </Style>
    <Style Selector="TextBlock.panel-title">
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
    </Style>
    <Style Selector="Border.surface">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Surface}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Lg}"/>
        <!-- 阴影经 BoxShadowsExtension 消费 token（BoxShadows 无 TypeConverter）。 -->
        <Setter Property="BoxShadow" Value="{helpers:BoxShadows {StaticResource Launcher.Elevation.Shadow.Md}}"/>
    </Style>
    <Style Selector="Border.remote-surface">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="ClipToBounds" Value="True"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.RemoteSurface.Margin}"/>
        <Setter Property="Width" Value="{StaticResource Launcher.Component.RemoteSurface.Width}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Component.RemoteSurface.MinHeight}"/>
        <Setter Property="MaxHeight" Value="{StaticResource Launcher.Component.RemoteSurface.MaxHeight}"/>
    </Style>
    <Style Selector="StackPanel.social-actions">
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.Xl}"/>
    </Style>
    <Style Selector="Border.remote-content-card">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Panel.Background}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Md}"/>
    </Style>
    <Style Selector="Border.info-strip">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Soft}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Md}"/>
    </Style>
    <Style Selector="Border.notice-card">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Notice.Background}"/>
        <Setter Property="MaxHeight" Value="{StaticResource Launcher.Component.NoticeCard.MaxHeight}"/>
    </Style>
    <Style Selector="Border.media-placeholder">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Media.Placeholder}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="Border.loading-overlay">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Overlay.Scrim.Md}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="UserControl.remote-content-loading-overlay">
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="ScrollViewer.remote-content-layout-host">
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Md}"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
    </Style>
    <Style Selector="Border.remote-content-load-error">
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Xl}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="TextBlock.media-placeholder-text">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.Media.Placeholder}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Sm}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="Border.path-field">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Field.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Field.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Field}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.PathField.Padding}"/>
    </Style>
    <!-- Operation panels share 40px horizontal alignment and common padding. -->
    <Style Selector="Border.bottom-panel">
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Component.BottomPanel.MinHeight}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Panel.Bottom.Background}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.BottomPanel.Padding}"/>
    </Style>
    <Style Selector="Grid.motion-overlay">
        <Setter Property="IsVisible" Value="False"/>
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Border.motion-surface">
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Grid.motion-surface-content">
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector=":is(UserControl).motion-content">
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="StackPanel.motion-content">
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Border.motion-bottom">
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <!-- ADR-016 壳层入场：窗口内容整体快速淡入一次，首帧即可交互；不分段错峰。 -->
    <Style Selector="Grid.motion-shell">
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <!-- ADR-016 壁纸淡出覆盖层：Source/Opacity 由视图后置代码在收到
         Background.PreviousWallpaperFadingOut 事件时驱动，不用属性过渡以免“先淡入旧图”。 -->
    <Style Selector="Grid.motion-shell.motion-enabled.motion-enter">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Fast}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Enter}">
                <KeyFrame Cue="0%">
                    <Setter Property="Opacity" Value="0"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="Opacity" Value="1"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <Style Selector="Grid.motion-overlay.motion-enabled.motion-enter">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Fast}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Enter}">
                <KeyFrame Cue="0%">
                    <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="Background" Value="{StaticResource Launcher.Color.Overlay.Scrim.Md}"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <Style Selector="Grid.motion-overlay.motion-enabled.motion-enter > Border.motion-surface">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Normal}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Enter}">
                <KeyFrame Cue="0%">
                    <Setter Property="TranslateTransform.Y" Value="{StaticResource Launcher.Motion.Offset.Surface}"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="TranslateTransform.Y" Value="0"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <!-- ADR-016：内部内容层不再二次运动；表面的单次进入/退出已由平行规则覆盖。 -->
    <Style Selector="Grid.motion-overlay.motion-enabled.motion-exit">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Fast}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Exit}">
                <KeyFrame Cue="0%">
                    <Setter Property="Background" Value="{StaticResource Launcher.Color.Overlay.Scrim.Md}"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <Style Selector="Grid.motion-overlay.motion-enabled.motion-exit > Border.motion-surface">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Fast}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Exit}">
                <KeyFrame Cue="0%">
                    <Setter Property="TranslateTransform.Y" Value="0"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="TranslateTransform.Y" Value="{StaticResource Launcher.Motion.Offset.Surface}"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <!-- DialogSurface 模板控件版本：整面滑移 + 整体淡入淡出（不再拆分内容层）。 -->
    <Style Selector="Grid.motion-overlay.motion-enabled.motion-enter > controls|DialogSurface.motion-surface">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Normal}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Enter}">
                <KeyFrame Cue="0%">
                    <Setter Property="TranslateTransform.Y" Value="{StaticResource Launcher.Motion.Offset.Surface}"/>
                    <Setter Property="Opacity" Value="0"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="TranslateTransform.Y" Value="0"/>
                    <Setter Property="Opacity" Value="1"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <Style Selector="Grid.motion-overlay.motion-enabled.motion-exit > controls|DialogSurface.motion-surface">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Fast}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Exit}">
                <KeyFrame Cue="0%">
                    <Setter Property="TranslateTransform.Y" Value="0"/>
                    <Setter Property="Opacity" Value="1"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="TranslateTransform.Y" Value="{StaticResource Launcher.Motion.Offset.Surface}"/>
                    <Setter Property="Opacity" Value="0"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <!-- ADR-016 内容切换：同级目的地纯交叉淡化，无方向位移。 -->
    <Style Selector=":is(UserControl).motion-content.motion-enabled.motion-enter">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Fast}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Enter}">
                <KeyFrame Cue="0%">
                    <Setter Property="Opacity" Value="0"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="Opacity" Value="1"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <Style Selector="StackPanel.motion-content.motion-enabled.motion-enter">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Fast}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Enter}">
                <KeyFrame Cue="0%">
                    <Setter Property="Opacity" Value="0"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="Opacity" Value="1"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <Style Selector="Border.motion-bottom.motion-enabled.motion-enter">
        <Style.Animations>
            <Animation Duration="{StaticResource Launcher.Motion.Duration.Normal}" FillMode="Forward" Easing="{StaticResource Launcher.Motion.Easing.Enter}">
                <KeyFrame Cue="0%">
                    <Setter Property="Opacity" Value="0"/>
                    <Setter Property="TranslateTransform.Y" Value="{StaticResource Launcher.Motion.Offset.Bottom}"/>
                </KeyFrame>
                <KeyFrame Cue="100%">
                    <Setter Property="Opacity" Value="1"/>
                    <Setter Property="TranslateTransform.Y" Value="0"/>
                </KeyFrame>
            </Animation>
        </Style.Animations>
    </Style>
    <Style Selector="Border.control-panel">
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Component.ControlPanel.MinHeight}"/>
        <!-- Dark gradient overlay on top of wallpaper — intentionally theme-invariant; value lives in Launcher.Color.ControlPanel.Gradient. -->
        <Setter Property="Background" Value="{StaticResource Launcher.Color.ControlPanel.Gradient}"/>
    </Style>
    <Style Selector="Border.content-row">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Md}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.ContentRow.Margin}"/>
    </Style>
    <Style Selector="Border.content-row.news-content-row">
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Sm}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="Button.content-link">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.content-link:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row.Hover}"/>
    </Style>
    <Style Selector="Button.content-link:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
    </Style>
    <Style Selector="Button.content-link:disabled">
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.banner-link">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="ZIndex" Value="1"/>
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
    </Style>
    <Style Selector="Button.banner-link:pointerover">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Chrome.Hover}"/>
    </Style>
    <Style Selector="Button.banner-link:pressed">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Chrome.Pressed}"/>
    </Style>
    <Style Selector="Button.banner-link:disabled">
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Grid.banner-item">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>
    <Style Selector="Border.banner-media">
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>
    <Style Selector="Border.banner-frame">
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>
    <Style Selector="Grid.banner-stage">
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>
    <Style Selector="Button.banner-control">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Text.OnChrome}"/>
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="ZIndex" Value="1"/>
    </Style>
    <Style Selector="Button.banner-control.carousel-navigation">
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.Sm}"/>
    </Style>
    <Style Selector="Border.banner-edge-gradient">
        <Setter Property="Width" Value="{StaticResource Launcher.Layout.Banner.EdgeGradient.Width}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="ZIndex" Value="1"/>
    </Style>
    <Style Selector="Border.banner-edge-gradient-left">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Banner.EdgeGradient.Left}"/>
    </Style>
    <Style Selector="Border.banner-edge-gradient-right">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Banner.EdgeGradient.Right}"/>
    </Style>
    <Style Selector="Grid.banner-control">
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="ZIndex" Value="1"/>
    </Style>
    <Style Selector="TextBlock.banner-control">
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="ZIndex" Value="1"/>
    </Style>
    <Style Selector="Grid.banner-stage.active > Button.banner-control">
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="IsHitTestVisible" Value="True"/>
    </Style>
    <Style Selector="Grid.banner-stage.active > Grid.banner-control">
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="IsHitTestVisible" Value="True"/>
    </Style>
    <Style Selector="Grid.banner-stage.active > Border.banner-edge-gradient">
        <Setter Property="Opacity" Value="1"/>
    </Style>
    <Style Selector="Grid.banner-stage.active > TextBlock.banner-control">
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
    </Style>
    <Style Selector="Button.banner-control:disabled">
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
    </Style>
    <Style Selector="Grid.banner-stage.active > Button.banner-control:disabled">
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Grid.settings-overlay">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Overlay.Scrim.Md}"/>
        <Setter Property="ZIndex" Value="100"/>
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Cycle"/>
        <Setter Property="views:OverlayFocusBehavior.IsEnabled" Value="True"/>
    </Style>
    <Style Selector="Grid.settings-workspace">
        <Setter Property="ColumnSpacing" Value="0"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Surface}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Settings.Navigation.List.Padding}"/>
    </Style>
    <Style Selector="Grid.settings-navigation-pane">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Surface}"/>
    </Style>
    <Style Selector="Border.settings-navigation-header">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Surface}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Component.Dialog.Title.Height}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Settings.Navigation.Header.Padding}"/>
    </Style>
    <Style Selector="Border.settings-navigation-header > StackPanel.dialog-title-row">
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Body}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.Settings.Navigation.Item.Margin}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Settings.Navigation.Item.Padding}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem materialIcons|MaterialIcon.settings-navigation-icon-filled">
        <Setter Property="IsVisible" Value="False"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected materialIcons|MaterialIcon.settings-navigation-icon-outline">
        <Setter Property="IsVisible" Value="False"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected materialIcons|MaterialIcon.settings-navigation-icon-filled">
        <Setter Property="IsVisible" Value="True"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected:not(:focus)">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:disabled">
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:pointerover /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:pressed /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected:not(:focus) /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected:pointerover /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
    </Style>
    <Style Selector="ListBox.settings-navigation > ListBoxItem:selected:pressed /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.SecondaryContainer.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnSecondaryContainer}"/>
    </Style>
    <!-- §8: visible tokenized focus ring for navigation items (FocusAdorner is null on the list). -->
    <Style Selector="ListBox.settings-navigation > ListBoxItem:focus-visible">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="TextBlock.settings-navigation-item">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="Grid.settings-content">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Dialog.Background}"/>
        <Setter Property="TextElement.Foreground" Value="{DynamicResource Launcher.Color.OnSurface}"/>
    </Style>
    <Style Selector="Border.settings-content-actions">
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Settings.Footer.Padding}"/>
    </Style>
    <Style Selector="Button.dialog-close.content-header-action">
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="Grid.settings-content-heading">
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.Settings.ContentHeading.Margin}"/>
    </Style>
    <Style Selector="StackPanel.settings-content-heading-copy">
        <Setter Property="Spacing" Value="{StaticResource Launcher.Spacing.Xs}"/>
    </Style>
    <Style Selector="TextBlock.category-subtitle">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Sm}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style Selector="StackPanel.settings-sections">
        <Setter Property="Spacing" Value="0"/>
    </Style>
    <Style Selector="StackPanel.settings-group">
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.Settings.Group.Margin}"/>
    </Style>
    <Style Selector="Border.settings-content-padding">
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Settings.Content.Padding}"/>
    </Style>
    <Style Selector="ScrollViewer.settings-content-scroll">
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Settings.Content.Scroll.Padding}"/>
    </Style>
    <Style Selector="Grid.dialog-overlay">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Overlay.Scrim.Md}"/>
        <Setter Property="ZIndex" Value="200"/>
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Cycle"/>
        <Setter Property="views:OverlayFocusBehavior.IsEnabled" Value="True"/>
    </Style>
    <Style Selector="Grid.setup-wizard-overlay">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Overlay.Scrim.Md}"/>
        <Setter Property="ZIndex" Value="500"/>
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Cycle"/>
        <Setter Property="views:OverlayFocusBehavior.IsEnabled" Value="True"/>
    </Style>
    <Style Selector="Grid.dialog-header">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Dialog.Header}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Component.Dialog.Title.Height}"/>
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>
    <Style Selector="StackPanel.dialog-title-row">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Spacing" Value="{StaticResource Launcher.Spacing.Md}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.Dialog.Title.Margin}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="ScrollViewer.dialog-scroll">
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="Border.dialog-footer">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Dialog.Footer}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Component.Dialog.Footer.BorderThickness}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Md}"/>
    </Style>
    <Style Selector="Grid.dialog-heading">
        <Setter Property="ColumnSpacing" Value="{StaticResource Launcher.Spacing.Md}"/>
    </Style>
    <Style Selector="StackPanel.dialog-heading-copy">
        <Setter Property="Spacing" Value="{StaticResource Launcher.Spacing.Xs}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="TextBlock.dialog-message">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Lg}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Body}"/>
    </Style>
    <Style Selector="Border.dialog-alert">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Info.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Info}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Component.Dialog.Alert.BorderThickness}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Md}"/>
    </Style>
    <Style Selector="Border.dialog-alert.warning">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Warning.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Warning}"/>
    </Style>
    <Style Selector="Border.dialog-alert.danger">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Danger.Soft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Danger}"/>
    </Style>
    <Style Selector="StackPanel.dialog-alert-heading">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Spacing" Value="{StaticResource Launcher.Spacing.Xs}"/>
    </Style>
    <Style Selector="TextBlock.dialog-alert-title">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Label.Md}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Info}"/>
    </Style>
    <Style Selector="TextBlock.dialog-alert-title.warning">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Warning}"/>
    </Style>
    <Style Selector="TextBlock.dialog-alert-title.danger">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Danger}"/>
    </Style>
    <Style Selector="materialIcons|MaterialIcon.dialog-alert-icon">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Info}"/>
    </Style>
    <Style Selector="materialIcons|MaterialIcon.dialog-alert-icon.warning">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Warning}"/>
    </Style>
    <Style Selector="materialIcons|MaterialIcon.dialog-alert-icon.danger">
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Danger}"/>
    </Style>
    <Style Selector="TextBlock.dialog-alert-content">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Body}"/>
    </Style>
    <Style Selector="Border.dialog-card">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Outline}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Lg}"/>
    </Style>
    <Style Selector="ListBox.update-file-list">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Md}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.UpdateFile.Margin}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:pointerover">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:selected">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.UpdateFile.Selected.Padding}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:pointerover /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:pressed /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:selected /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:selected:not(:focus) /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:selected:pointerover /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="ListBox.update-file-list > ListBoxItem:selected:pressed /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <!-- §8: visible tokenized focus ring for the update file list items. -->
    <Style Selector="ListBox.update-file-list > ListBoxItem:focus-visible">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="Border.update-file-icon">
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Soft}"/>
    </Style>
    <Style Selector="Border.update-file-size">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.UpdateFile.Size.Padding}"/>
    </Style>
    <Style Selector="TextBlock.update-file-name">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Sm}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="TextBlock.update-file-size-text">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Label.Md}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <Style Selector="Border.dialog-icon">
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Soft}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
    </Style>
    <Style Selector="Border.dialog-icon.danger">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Danger.Soft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Danger}"/>
    </Style>
    <Style Selector="TextBlock.section-title">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="TextBlock.group-title">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
    </Style>
    <Style Selector="TextBlock.category-title">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Headline.Md}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="TextBlock.chip-text">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Label.Md}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="StackPanel.button-content">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Spacing" Value="{StaticResource Launcher.Spacing.Sm}"/>
    </Style>
    <Style Selector="StackPanel.button-content > TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="StackPanel.card-heading">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Spacing" Value="{StaticResource Launcher.Spacing.Sm}"/>
    </Style>
    <Style Selector="Border.settings-section">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Lg}"/>
    </Style>
    <Style Selector="Grid.settings-row">
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Component.Settings.Row.MinHeight}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="ColumnSpacing" Value="{StaticResource Launcher.Spacing.Md}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="Border.settings-row-divider">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Component.Dialog.Footer.BorderThickness}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>
    <Style Selector="StackPanel.settings-row-copy">
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <!-- 关于分区（ADR-018 融合变体）：左对齐身份卡 + key-value 详情行 + chevron 链接行。 -->
    <Style Selector="Border.about-identity-card">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Lg}"/>
    </Style>
    <Style Selector="TextBlock.about-product-name">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Headline.Lg}"/>
        <Setter Property="FontWeight" Value="{StaticResource Launcher.Typography.FontWeight.Strong}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="Border.about-kv-list">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>
    <Style Selector="Grid.about-kv-row">
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Component.About.KvRow.MinHeight}"/>
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.About.KvRow.Margin}"/>
        <Setter Property="ColumnSpacing" Value="{StaticResource Launcher.Spacing.Md}"/>
    </Style>
    <Style Selector="Grid.about-kv-row > TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="TextBlock.about-kv-value">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Sm}"/>
        <Setter Property="FontFamily" Value="{StaticResource Launcher.Typography.FontFamily.Monospace}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="HorizontalAlignment" Value="Right"/>
        <Setter Property="TextAlignment" Value="Right"/>
    </Style>
    <Style Selector="ItemsControl.about-link-list">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
    </Style>
    <Style Selector="Button.about-link-item">
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.About.KvRow.Margin}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Component.About.KvRow.MinHeight}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style Selector="Button.about-link-item:pointerover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
    </Style>
    <Style Selector="Button.about-link-item:pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="RenderTransform" Value="none"/>
    </Style>
    <Style Selector="Button.about-link-item:disabled">
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <!-- Debug design gallery: a compact 3×6 M3 component state matrix. -->
    <Style Selector="Border.design-state-matrix-card">
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.Lg}"/>
    </Style>
    <Style Selector="Grid.design-state-matrix">
        <Setter Property="ColumnSpacing" Value="{StaticResource Launcher.Spacing.Sm}"/>
        <Setter Property="RowSpacing" Value="{StaticResource Launcher.Spacing.Sm}"/>
    </Style>
    <Style Selector="TextBlock.design-state-header">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Label.Sm}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>
    <Style Selector="TextBlock.design-state-component">
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Label.Md}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="Button.gallery-button">
        <Setter Property="Template" Value="{StaticResource LauncherBorderButtonTemplate}"/>
        <Setter Property="Width" Value="{StaticResource Launcher.Component.DesignGallery.StateCell.Width}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.DesignGallery.StateCell.Padding}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>
    <Style Selector="Button.gallery-button.state-hover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="Button.gallery-button.state-pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Button.gallery-button.state-disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.gallery-button.state-focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="Button.gallery-button.state-invalid">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Danger.Soft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Danger}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Danger}"/>
    </Style>
    <!-- Gallery matrix: M3 four-type button variants (ADR-007/ADR-004). -->
    <Style Selector="Button.gallery-button.outlined">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Button.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
    </Style>
    <Style Selector="Button.gallery-button.outlined.state-hover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.gallery-button.outlined.state-pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.gallery-button.outlined.state-disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.gallery-button.outlined.state-focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="Button.gallery-button.text">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.gallery-button.text.state-hover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.gallery-button.text.state-pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Button.gallery-button.text.state-disabled">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.gallery-button.text.state-focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="Button.gallery-button.error">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.gallery-button.error.state-hover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error.Hover}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.gallery-button.error.state-pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Error.Pressed}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Color.OnError}"/>
    </Style>
    <Style Selector="Button.gallery-button.error.state-disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.gallery-button.error.state-focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="ComboBox.gallery-select">
        <Setter Property="Width" Value="{StaticResource Launcher.Component.DesignGallery.StateCell.Width}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Field.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Field.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Select.Padding}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="ComboBox.gallery-select.state-hover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="ComboBox.gallery-select.state-pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="ComboBox.gallery-select.state-disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Secondary}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="ComboBox.gallery-select.state-focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="ComboBox.gallery-select.state-invalid">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Danger.Soft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Danger}"/>
        <Setter Property="Foreground" Value="{StaticResource Launcher.Color.Danger}"/>
    </Style>
    <Style Selector="Border.gallery-card">
        <Setter Property="Width" Value="{StaticResource Launcher.Component.DesignGallery.StateCell.Width}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.DesignGallery.StateCell.Padding}"/>
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Card.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
    </Style>
    <Style Selector="Border.gallery-card.state-hover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="Border.gallery-card.state-pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Border.gallery-card.state-disabled">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Content.Row}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Border.gallery-card.state-focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="Border.gallery-card.state-invalid">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Danger.Soft}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Danger}"/>
    </Style>
    <!-- Gallery matrix: Toast-styled card (ADR-007 "card (incl. Toast card)"). -->
    <Style Selector="Border.gallery-card.toast">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Toast.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
    </Style>
    <Style Selector="Border.gallery-card.toast.state-hover">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="Border.gallery-card.toast.state-pressed">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Button.Flat.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Border.gallery-card.toast.state-focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.FocusRing}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Focus}"/>
    </Style>
    <Style Selector="ColorPicker.setting-control">
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Component.Settings.Control.MinWidth}"/>
        <Setter Property="Width" Value="{StaticResource Launcher.Component.Settings.Control.MinWidth}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Field}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    <Style Selector="TextBox.uid-input">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Field.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Field.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Md}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Input.Padding}"/>
        <Setter Property="FontSize" Value="{StaticResource Launcher.Typography.FontSize.Body.Md}"/>
        <Setter Property="Foreground" Value="{DynamicResource Launcher.Text.Primary}"/>
        <Setter Property="MinWidth" Value="{StaticResource Launcher.Component.Input.MinWidth}"/>
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Field}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="CaretBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="TextBox.uid-input:focus">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="TextBox.uid-input:pointerover">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="ToggleSwitch:checked /template/ Border#SwitchKnobBounds">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="ToggleSwitch.setting-toggle">
        <Setter Property="MinHeight" Value="{StaticResource Launcher.Control.Height.Setting}"/>
    </Style>
    <Style Selector="ToggleSwitch:checked:pointerover /template/ Border#SwitchKnobBounds">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Hover}"/>
    </Style>
    <Style Selector="ToggleSwitch:checked:pressed /template/ Border#SwitchKnobBounds">
        <Setter Property="Background" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="ToggleSwitch:checked /template/ Ellipse#SwitchKnobOn">
        <Setter Property="Fill" Value="{DynamicResource Launcher.Color.OnPrimary}"/>
    </Style>
    <Style Selector="Border.color-swatch">
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Size.Swatch}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Size.Swatch}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
    </Style>
    <Style Selector="Button.color-swatch-button">
        <Setter Property="Background" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Launcher.Color.Transparent}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
        <Setter Property="Width" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Control.Height.Setting}"/>
        <Setter Property="Padding" Value="{StaticResource Launcher.Spacing.Thickness.None}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
    </Style>
    <Style Selector="Button.color-swatch-button:pointerover">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary}"/>
    </Style>
    <Style Selector="Button.color-swatch-button:pressed">
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Primary.Pressed}"/>
    </Style>
    <Style Selector="Button.color-swatch-button:disabled">
        <Setter Property="Opacity" Value="{StaticResource Launcher.StateLayer.Disabled.Content}"/>
    </Style>
    <Style Selector="Button.wrap-action">
        <Setter Property="Margin" Value="{StaticResource Launcher.Component.WrapAction.Margin}"/>
    </Style>
    <!-- Shared utility styles for repeated inline patterns -->
    <Style Selector="ScrollViewer.scroll-pad">
        <Setter Property="Padding" Value="{StaticResource Launcher.Component.Scroll.Pad}"/>
    </Style>
    <Style Selector="Border.color-swatch-preview">
        <Setter Property="Width" Value="{StaticResource Launcher.Component.ColorSwatch.Preview.Size}"/>
        <Setter Property="Height" Value="{StaticResource Launcher.Component.ColorSwatch.Preview.Size}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Launcher.Radius.Sm}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Launcher.Color.Card.Border}"/>
        <Setter Property="BorderThickness" Value="{StaticResource Launcher.Border.Thickness.Default}"/>
    </Style>
    <!-- Semantic marker classes used by layout and headless UI contracts. -->
    <Style Selector="Button.news-row"/>
    <Style Selector="ScrollViewer.news-viewport"/>
    <Style Selector="StackPanel.operation-actions"/>
    <Style Selector="Grid.operation-layout"/>
    <Style Selector="Grid.operation-status"/>
    <Style Selector="Button.primary-operation"/>
    <Style Selector="Button.secondary-operation"/>
    <StyleInclude Source="avares://Cafe.Launcher.Avalonia/Views/Styles/Diagnostics.axaml"/>
    <StyleInclude Source="avares://Cafe.Launcher.Avalonia/Views/Styles/RemoteContent.axaml"/>
    <StyleInclude Source="avares://Cafe.Launcher.Avalonia/Views/Styles/SetupWizard.axaml"/>
    <StyleInclude Source="avares://Cafe.Launcher.Avalonia/Views/Styles/Toast.axaml"/>
    <StyleInclude Source="avares://Cafe.Launcher.Avalonia/Views/Styles/DialogSurface.axaml"/>
</Styles>
