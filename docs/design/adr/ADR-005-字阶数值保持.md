# ADR-005: 字阶数值保持现状（§3.5）

- 状态：**已接受**（2026-08-25，P2 决策 Round 1）
- 背景：字号 11–22；M1 已归入 `Typography.FontSize.{Display,Headline.*,Title.*,Body.*,Label.*}` 角色键。
- 决策：**保持现状字号数值**；P2 只做 `Typography.*` 角色键的全量消费迁移（随组件批次走），不调数值。
- 后果：与黄金基线解耦；M3 标准字号差异记录为有意搁置；P3 表面阶段再评估。
