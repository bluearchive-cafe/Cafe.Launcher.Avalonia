# ADR 0001：本地安装状态是启动器事实来源

状态：已接受

启动器以游戏目录中的本地安装状态和清单为已安装版本、文件集合与启动参数的事实来源。主界面路径显示、持久化设置和安装工作流必须使用同一个规范化路径快照；设置页仅编辑草稿，主界面路径操作立即规范化、保存并重新加载快照。

不得从目录名、可执行文件名或远端响应推断本地安装状态。读取失败必须返回结构化的 `LocalInstallationStateKind`，由上层决定安装、修复或提示。
