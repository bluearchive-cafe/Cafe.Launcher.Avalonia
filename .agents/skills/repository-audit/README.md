# repository-audit skill package

Install the `repository-audit/` directory into your agent skill directory. `SKILL.md` is intentionally short; domain detail is loaded from `references/` only when relevant.

Run `python scripts/validate-skill.py` after edits. Regression scenarios are in `tests/scenarios.md`.
