## v1.1.0-beta.9

![Cafe Launcher v1.1.0-beta.9 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.9/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.9-release-banner.png)

> [!NOTE]
> 本版本聚焦「网络更稳、反馈更好给」：启动校验发现游戏文件损坏时会直接弹出修复入口，不再只丢一句警告；导出诊断信息可以只保留最近一段时间、只勾选要带上的内容；下载与校验在代理、重定向、网络停滞这些异常情况下不再卡死或直接失败，HTTP/2 已默认启用并可在设置里关闭。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.8`，这些向后兼容的新特性与缺陷修复将预发布号递增为 `v1.1.0-beta.9`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。macOS 仅支持安装、更新和修复游戏，暂不支持启动游戏。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

### 游戏启动与修复

- **启动校验发现文件损坏时，可以直接修复**：以前启动校验查出游戏文件损坏，只弹一条警告，要自己进设置发起修复。现在会弹出修复确认，并说明它为什么出现——「启动校验发现游戏文件已损坏。修复将检查所有游戏文件并重新下载损坏或丢失的文件。这会耗费一段时间。」点「确认修复」即开始；修复完成与「没有需要修复的文件」都有明确结果。

- **启动游戏后确实会最小化到托盘**：提示语一直写着「游戏正在启动 — 启动器已最小化到托盘」，实际只做了任务栏最小化——窗口仍留在任务栏上，托盘里也没有图标，等于让人去找一个不存在的东西。现在按「有托盘就隐藏到托盘，没有托盘才退回任务栏」的既有规则执行。标题栏的最小化按钮不变，仍然是普通最小化。

### 诊断与日志

- **导出诊断信息可以只导需要的部分**：导出对话框新增「时间范围」——全部 / 最近 1 小时 / 最近 24 小时 / 最近 7 天 / 最近 30 天，以及「包含内容」——日志文件与系统信息始终包含，崩溃报告与用户数据可选。日志查看器、设置「高级」与调试面板三个入口共用同一个对话框。导出前会先探测所选范围内有没有内容，为空时给出提示但不阻止导出；导出过程可以取消，只有完整写入的文件才会出现在目标位置，导出失败也不会再被报成成功。

- **日志条目更好按模块翻阅**：日志条目标题以前一半是模块名、一半是英文整句，现在统一为简短的模块标签，细节移进条目正文；这些标题正是日志查看器里显示的小标题，条目信息量不变。

- **不再记录并未发生的「下载已停止」**：干净退出时，日志里以前固定出现两条「用户停止了下载」，而当时并没有任何下载在运行（退出时会走两次停止流程，两次都没有在跑的下载）。现在只有真的停掉了一个下载才记录；退出时确有下载在跑，也从两条变为一条。

- **导出日志的入口图标与导出面板统一**：日志查看器与设置「高级」里的「导出日志」按钮，图标改为与它们打开的导出面板一致，同一个动作不再出现两种图标。

### 网络与下载

- **新增「启用 HTTP/2」设置（默认开启）**：新请求优先使用 HTTP/2，不可用时自动回退到 HTTP/1.1，位置在设置「下载与网络」。

- **网络停滞不再让下载卡死**：读取响应正文时若 60 秒没有任何数据（CDN 或家用网络设备静默丢弃连接时的常见形态），以前会一直挂起，且不触发任何重试。现在会按可重试的网络错误处理，自动换源或续传，已经下载的部分不会白费。

- **SOCKS 代理可以正常使用**：代理写成 `socks://`（或系统里遗留的 `socks=` 条目）时，第一个经代理的请求就会以「不支持该代理方案」失败；现在统一识别为 `socks5://`。

- **自更新检查更可靠**：检查更新在代理端点超时或返回失败时会落到 GitHub Releases 兜底——超时恰恰是最需要备用源的那种故障；两个端点的重定向都会逐跳复验，不再因为服务器的一次重定向就直接失败。

- **资源面板恢复可用**：资源面板的请求改走与其他网络请求相同的重定向路径，每一跳都复验，此前服务器返回重定向时会直接失败并做无用重试；服务器明确拒绝（维护、限流）也不再重试三次。

- **下载与校验更快**：连接超时从运行时默认的 100 秒收紧到 15 秒，连不上的地址不再先耗尽整个请求预算；首屏的多个远端读取加了 30 秒整体预算，到点即按既有降级路径显示远端不可用；主机 DNS 校验结果按 30 秒缓存，全新安装的万级文件不再逐个重新解析；CRC-64 校验缓冲改为复用，不再为每个文件分配 1 MiB；在健康安装上执行修复时，文件不再被完整读两遍。

### 安全与隐私

- **导出的日志不再带上玩家 UID**：资源面板把玩家 UID 放在网址的查询参数里，而网络错误信息会把完整网址写进日志，导出的日志又始终包含这些内容。现在诊断信息只保留路径，查询串一律去掉。

- **崩溃报告文件里不再出现你的用户目录**：崩溃报告文档中记录自身位置的字段，此前会把完整路径（含 Windows 账户名）写进文件，而导出的诊断会带上这些报告。现在该字段不再落盘；崩溃编号、时间、版本与构建信息都不受影响。隐私说明（PRIVACY.md）承诺的用户目录替换，现在对报告文档里的所有字段都成立。
