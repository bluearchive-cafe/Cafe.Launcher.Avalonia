## v1.1.0-beta.4

![Cafe Launcher v1.1.0-beta.4 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.4/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.4-release-banner.png)

> [!NOTE]
> 本版本完成跨平台运行环境的正确性加固、健康检查与桌面集成：运行环境可用性检查与实际启动使用同一份配置，可用性升级为真实版本探测与分级诊断；Linux 设置页新增运行环境状态与高级路径设置；新增 `--launch-game` 命令行与 Linux 桌面快捷方式，Windows 桌面快捷方式改指向游戏目录的 `run.bat` 启动脚本，不经启动器、可直接启动游戏。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.3`，这些向后兼容的新增与修复将预发布号递增为 `v1.1.0-beta.4`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

### 运行环境正确性与健康检查

- **可用性检查与启动共用同一份配置**：运行环境可用性检查现在按用户设置的运行器路径评估；显式配置的路径无效时直接报错，不再静默回退 PATH，避免"配置写错却用另一个运行时启动"。

- **修复设置文件损坏导致的启动崩溃**：手工编辑或旧版本残留的 `"gameRuntime": null` 不再使启动器加载设置时崩溃，将自动恢复默认运行环境配置。

- **健康检查升级为真实探测**：UMU 与 Wine 的可用性从"文件存在"升级为执行 `--version` 的真实探测（带超时与进程树清理），并区分不支持、未找到、无法运行与可用四种状态；可用的运行环境会报告可执行文件路径与版本号。

- **启动诊断包含完整运行环境快照**：启动诊断现在记录所选运行器、版本、可执行文件、生效 Prefix 与 Proton 目标，便于把故障定位到正确层级。

### 稳定运行时标识与数据隔离

- **引入稳定运行时标识**：兼容环境状态改用固定标识 `blue-archive-jp`，不再依赖游戏可执行文件名，重命名 EXE 不会孤立已有兼容数据。

- **Prefix 按运行器隔离**：UMU 与 Wine 使用各自独立的默认 Prefix（`compatibility/<游戏标识>/<运行器>/prefix`），切换运行器不再互相污染；旧布局不会自动迁移，需要沿用既有环境的用户请显式配置 Prefix 路径。

### Linux 设置体验

- **运行器列表移除 Native**：Linux 设置页不再显示必然不可用的原生启动选项；历史遗留的原生设置自动归一化为自动模式。

- **新增运行环境状态显示**：设置页可实时查看各运行环境的可用性、可执行文件与版本号（真实探测）。

- **新增高级路径设置**：可配置运行器、Prefix 与 Proton 路径，留空时以占位文案展示默认管理策略（自动检测 / 由 Cafe Launcher 管理 / 由 UMU 自动选择）。

- **修复保存按钮不生效的问题**：此前更改兼容运行环境相关设置不会激活设置保存按钮（脏检查遗漏嵌套字段），已修复。

### 桌面集成

- **新增 `--launch-game` 命令行**：`cafe-launcher --launch-game` 直接启动游戏，仍走清单校验、更新检查、clickCode、运行器选择与诊断的完整管线；已有启动器实例时自动转发启动请求，无竞态丢失。首次安装的设置向导会话不会自动启动游戏，完成初始化后即可正常使用。

- **Linux 桌面快捷方式**：可生成符合 freedesktop 规范的 `.desktop` 入口，经 `--launch-game` 走启动器完整管线，图标取自启动器资源并写入可执行位。

- **Windows 快捷方式改指向 run.bat**：桌面快捷方式的目标从游戏 EXE 改为游戏目录自带的 `run.bat` 启动脚本（直接运行游戏 EXE 无法启动游戏），不经启动器、行为与直接运行 `run.bat` 一致；`run.bat` 缺失时视为游戏未就绪。

### 关于界面与工程质量

- **关于分区重设计**：重设计关于分区并完善版本信息展示。

- **文档与社区**：重写项目说明与开发指南，新增 GitHub Bug 与功能建议模板。

- **UI 修复**：修复重置设置后底部操作区空白；对齐设置页危险操作按钮尺寸。

- **补充回归测试**：新增运行环境配置一致性、健康探测、诊断快照、Prefix 布局、设置脏检查、桌面入口生成与快捷方式目标的回归测试；四语言资源键与占位符契约保持同步。
