## v1.0.0

> [!NOTE]
> 本版本聚焦于窗口覆盖层动画的稳定性，以及下载状态跨线程同步与清理流程的回归保障。

### 修复

- **对话框过渡动画**：修复对话框、设置页、日志查看器、调试面板和安装向导覆盖层在进入/退出时的动画合成异常。遮罩背景、面板位移和面板内容透明度现在分别执行动画，避免整体透明度与位移动画相互影响。

- **覆盖层动画时序**：移除覆盖层进入动画中不必要的延迟，使背景和面板内容按照稳定的时序完成过渡。

### 测试与稳定性

- **下载状态线程同步**：补充后台线程触发下载状态变化时的 UI 线程通知测试，并验证最新状态能够覆盖过期的后台通知。

- **资源清理回归**：覆盖操作对象释放后忽略旧旅程回调的场景，避免已销毁对象继续更新界面。

- **进度更新回归**：验证后台线程提交下载进度后，进度面板和进度值会在 UI 线程调度完成后正确刷新。

## v1.0.0-beta.10

> [!NOTE]
> 本版本集中提升游戏下载流程与窗口状态同步的可靠性：下载速度、进度及完成状态会更准确地反映实际情况；修复、暂停、恢复、停止和卸载等操作不再因重复消息而被重复处理。同时优化了退出清理与社交媒体按钮的交互视觉。

### 修复

- **下载速度显示**：速度统计现在会累计完整的限频传输窗口，并依据最近采样窗口计算实际速度，避免显示值偏离真实下载速率。

- **下载完成状态**：游戏下载完成后会正确切换为就绪状态，不会继续显示为“下载中”；刷新过程中也不会让旧状态覆盖刚开始的新操作。

- **续传与重试进度**：暂停后恢复、断点续传和文件校验重试时，进度与限速统计会连续且准确，不会重复计算或倒退。

- **重复游戏操作**：修复流程、卸载、停止下载及操作提示的重复订阅已清理，避免一次用户操作触发两次修复或重复处理。

- **窗口状态同步**：恢复游戏下载运行状态在下载工作流、操作旅程和主界面之间的消息转发；过期的后台通知不会再覆盖最新状态。

- **退出与覆盖层清理**：退出前会取消并等待状态刷新任务完成，关闭窗口或解绑界面时会释放对应消息通道，避免卸载确认框残留或旧协调实例继续响应。

- **社交媒体按钮状态**：禁用和悬停时采用统一的清晰边框模板，消除按钮边缘发虚的问题。

### 优化

- **下载状态刷新**：状态刷新改为串行执行，并按注册顺序等待异步消息处理完成，降低快速切换下载状态时的竞态与界面闪动。

- **生命周期资源管理**：明确依赖注入与手动创建对象的释放所有权，减少窗口重建或应用退出时遗留的事件订阅和资源占用。
