## v1.1.0-beta.8

![Cafe Launcher v1.1.0-beta.8 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.8/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.8-release-banner.png)

> [!NOTE]
> 本版本聚焦「崩溃之后看得见、失败之后说得清」：不可恢复的崩溃现在会弹出独立的报告窗口，带报告编号、可复制的技术详情与「打开日志目录」入口；启动失败提示改为说明具体原因（哪个运行环境、为什么不适用），不再是一句通用文案；内置壁纸不再压在启动关键路径上同步解码；macOS 的支持范围明确为「只能安装、更新和修复，暂不支持启动游戏」。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.7`，这些向后兼容的缺陷修复与体验改进将预发布号递增为 `v1.1.0-beta.8`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

### 崩溃诊断

- **不可恢复崩溃报告窗口**：此前崩溃后进程直接消失，用户无从判断发生了什么。现在引入不可恢复崩溃边界：主界面健康时在同进程显示崩溃报告窗口，入口逃逸、调度器、AppDomain 与诊断初始化等进程边界故障先写入崩溃快照、再以最小化报告模式重新拉起；窗口只提供三个动作——「打开日志目录 / 复制详情 / 退出启动器」，默认展示友好摘要，技术详情默认折叠；摘要行含报告编号、时间、版本与构建 SHA，致命日志条目同样携带构建 SHA。同一次进程只把首个崩溃作为主报告，后续失败追加旁路日志，所有关闭路径都以非零退出码结束。

- **崩溃快照更稳健、披露更完整**：快照只记录异常、应用版本、构建 SHA、操作系统与 UI 语言，用户目录统一替换为 `%USERPROFILE%`；主目录不可写时降级到临时目录，两处都写不进时仍会显示「快照不可读」的报告窗口。修复了快照缺少 UI 语言时报告进程静默退出、不显示任何窗口的缺陷——现在任何一次不可恢复崩溃都有界面。PRIVACY.md 同步补上崩溃快照这一本地数据类别，以及 CrashReports 目录 10 份 / 30 天的保留策略。

### 启动与游戏管理

- **启动失败提示说明具体原因**：此前「没有可用运行环境」的启动失败只提示通用文案（游戏进程未能启动），具体原因（平台不支持 / 运行时缺失 / 运行时损坏）只写进诊断日志，用户看不出为什么。现在提示会给出本地化的运行环境名与状态，例如「没有可用的运行环境。Windows（原生）：当前平台不支持。」；候选选择规则为「已固定的运行环境优先，否则取第一个缺失或损坏的（可操作），再否则取第一个（通常是平台不支持）」，诊断日志仍保留完整候选列表。

- **启动期不再同步解码内置壁纸**：2560×1388 的内置壁纸此前在 DI 构造期、首帧之前在 UI 线程同步解码，且随后首次刷新还会再解码一次。现在构造期不做解码，初始壁纸由首次刷新在线程池解码后填入，窗口在此期间显示主题底色——同一张壁纸在启动期只解码一次。

### 设置

- **「重置设置」确认的 Escape 行为**：该确认框此前是唯一没有纳入模态栈的模态表面，按 Escape 会关掉下层的设置页而不是取消确认。现在它与其他模态一致：Escape 取消确认，交互门禁一并生效。

### 平台支持

- **macOS 支持范围明确**：macOS 版本目前只能安装、更新和修复游戏；启动游戏需要额外的兼容运行层，当前不提供，也暂无支持计划。该表述已写入 README 与文档站。

### 安全与可靠性

- **清单路径校验收紧**：拒绝会归一到游戏根目录的清单条目（空路径、`.`、`sub/..` 等）。此前这类条目会让下载临时文件写到游戏目录之外（`<游戏目录>.tmp`），安装阶段再以异常收尾；现在下载临时名、安装校验与删除共 7 处调用点统一拒绝。

- **网络响应体上限**：JSON 响应体缓冲加 64MiB 上限（Content-Length 预检 + 分块流式累计钳制），异常响应不再可能无限占用内存。

- **静默失败补诊断日志**：点击码写入的三处失败与资源面板读取 Cookie 库的失败此前被静默吞掉，现在记为 Warn，便于用户反馈时定位。

### 工程质量

- **测试守卫补齐**：清单 JSON 键序与设置深克隆完整性、卸载安全闸口（游戏运行中 / 驱动器根 / 路径不存在）、模态 Escape 路由改为枚举全部模态、约 15 处测试等待加 5 秒上限（回归会失败而不是挂住 CI）。

- **审计收尾**：2026-09-08 / 09 的 full 与 delta 审计发现全部核销，台账 57 项、0 open，剩余 4 项为有意暂缓或接受风险。
