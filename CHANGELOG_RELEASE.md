## v1.1.0-beta.11

![Cafe Launcher v1.1.0-beta.11 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.11/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.11-release-banner.png)

> [!NOTE]
> 本版本围绕三件事：让启动器把自己更新得干净利落、让游戏启动后的每一步都有回应、把 Linux 实验构建补齐。
>
> 这是 `1.1.0` Beta 预发布序列中的一次向后兼容更新，稳定版号 `1.1.0` 不变。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。
> - macOS 仅支持安装、更新和修复游戏，启动游戏暂无计划支持。
> - Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

---

### 本版亮点

- **Windows 上可以把更新交给启动器自己做完**：发现新版本后直接在应用内下载、校验并安装，点「重启以更新」就回到最新版本。
- **游戏启动后的每一步都有回应**：控制面板与托盘的会话状态行随游戏从「启动中」走到「运行中」或「已退出」，运行器提前退出也会立刻恢复窗口并说明原因。
- **Linux 实验构建补齐**：运行中保护真正生效，启动前检查兼容环境，资源面板能读出 UID，诊断信息更全。
- **此外，安装、卸载与日常交互上的粗糙处一并收拾**：只读文件不再让整次卸载失败，清理旧文件时可以立刻「停止」，失败与网络提示也都给了下一步去处。

---

### 更新启动器

- **发布说明在「发现新版本」对话框里按完整排版预览**：「发现新版本」对话框直接显示该版本的发布说明，标题、列表、链接与引用都按完整排版呈现，启动时检查到新版本的提示条上也多了「查看更新」按钮，点它即可打开这个对话框。
  - 提示块与任务列表也能正确显示，浅色与深色主题各自适配。

- **下载、校验与安装都由启动器接着做完**：点「下载」由启动器自己完成下载，进度条与状态行同时给出百分比与实时速度，校验通过后提示「更新已就绪，重启以应用。」。
  - 更新包先按发布页的 SHA-256 摘要清单校验，校验不通过不会安装。

- **点「重启以更新」就装好并回到启动器**：启动器退出、自动完成安装并重新打开，全程不需要手动下载或运行安装程序，安装版与便携版都支持。
  - 下载、校验或启动安装程序失败时（分别提示「更新下载失败，请检查网络后重试。」与「无法启动更新，请稍后再试。」），对话框的主按钮换成「前往发布页」；主动取消下载则回到原样。

- **不能应用内更新时，先说清是哪一种原因，再直接前往发布页**：对话框会按真实原因给话——macOS 与 Linux 上说明「当前平台不支持在应用内更新」，本机缺更新组件的安装说明「此安装缺少更新组件…（下载完整安装包后即可恢复）」，该版本没有可校验的安装包则说明启动器不会无校验执行更新。主按钮都是「前往发布页」，点击即用浏览器打开发布页，下载安装包由你手动完成——不再先让你下载一整个安装包、到最后一步才发现装不上，也不再把这三种情况笼统说成「设备不行」。

- **检查更新失败不再闪退**：更新源地址未通过安全校验时，现在提示「无法检查 Cafe Launcher 更新。」，不再直接崩溃退出。

### 安装、修复与卸载

- **只读文件不再让卸载整体失败**：手工拷贝进来的、被打过更新包标记的只读文件现在都能正常删除，不再让整次卸载中止到一个文件也删不掉。
  - 确实删不掉的照旧逐条列在完成提示里：「卸载完成。以下项目无法删除，可能需要重启后才能移除：…」。

- **清理旧文件时也能「停止」**：安装与更新在替换文件之前要先删掉不再需要的旧文件，现在按下「停止」会立即停下。
  - 此前这一段删除无法取消，按下「停止」也得等这一批删完。

### 启动游戏与会话

- **游戏启动后的状态看得见了**：控制面板多出一条会话状态行，随游戏显示「游戏启动中」「游戏运行中」，游戏结束后换成「游戏已退出（退出码 X）」。
  - 正常退出不打扰、只换状态行，没有会话时整行隐藏。

- **运行器提前退出时会先把窗口恢复出来再说明原因**：用第三方运行器（Wine/Proton）启动的会话同样会更新这条状态行；如果运行器在游戏进程出现之前就退出（例如前缀损坏、缺少依赖），启动器会先把主窗口从托盘恢复出来，再提示「游戏未能启动（退出码 X）」。
  - 不再留下没有下文的「游戏正在启动」。

- **启动校验被跳过时会说明**：启动校验选择远程文件清单但获取失败时，状态行显示「无法获取远程文件清单，已跳过本次启动校验。」，不再无声跳过。

- **不再卡在「正在生成文件列表」**：判断游戏是否正在运行时的进程扫描现在有时间上限，扫不出来时改为提示「无法确认游戏是否正在运行。请关闭游戏后重试。」。
  - 等待期间取消也立即生效，不再一直转圈且取消不掉。

### 托盘与窗口

- **托盘菜单新增「开始游戏」与「设置」**：右键托盘图标可以直接开始游戏或打开设置，不必先打开主窗口。
  - 游戏启动中或运行中时，「开始游戏」一项会显示当前会话状态（「游戏启动中」「游戏运行中」）；两处入口在需要时会先把主窗口恢复出来，让确认框与提示看得见。

- **从托盘恢复不再重复提示**：通过托盘恢复窗口时不再重走一遍带启动参数的流程，「游戏正在启动」提示不会重复弹出。
  - 退出启动器时也不再把托盘图标释放的信号误记为一次崩溃。

### 界面与交互

- **失败可以就地处理**：公告与远程内容加载失败时，失败卡片上直接提供「重试」按钮，不必重新进入页面。
  - 卡片补上了底色与圆角，不再透出壁纸。

- **安装与更新失败时能直接看日志**：安装、更新等操作以失败告终时，提示上直接提供「查看日志」入口。

- **提示条悬停暂停**：提示条的停留时长改为按类型固定的档位，鼠标悬停时暂停计时，读完再消失，长提示不再读一半就没了。

- **底部显示下载源**：主界面底部状态区显示当前使用的下载源（「下载源：××」），加载中也会标出。

- **UID 生成提示**：资源面板在 UID 尚未生成时常驻显示一条说明——「通过 Cafe 下载源首次启动游戏后，才会生成 UID。」

- **游戏设置页路径旁新增「打开游戏目录」**：点它直接打开游戏所在的目录。

- **设置项重新归组**：更新通道并入常规页「更新」分组，状态面板与远程内容卡片移入常规页「显示」分组，运行器/前缀/Proton 路径并入游戏页「兼容运行环境」分组，外观页只留主题与背景。

- **游戏操作主按钮的图标跟着动作走**：安装显示下载、修复显示工具、刷新显示刷新、更新显示更新，不再固定是同一个下载图标。
  - 应用图标进一步调整；设置向导「跳过」按钮与路径选择按钮换用更贴切的图标；日志查看器顶端间距修正，与过滤栏的间距不再随内容滚动；「游戏正在运行」提示里的进程名不再重复一遍 `.exe` 后缀。

### 网络与代理

- **网络错误提示更友好**：网络请求失败时提示「网络请求失败，请检查网络或代理设置后重试。」。
  - 原始错误信息只进日志与诊断导出，不再整句抛给用户。

- **适配 Fake-IP 模式的代理软件**：使用 Clash、mihomo 等以 Fake-IP 模式接管 DNS 的代理软件时，直连路径上解析结果落在 Fake-IP 保留段（IPv4 198.18.0.0/15、IPv6 fc00::/7）不再被当作异常拦截。
  - 若随后仍连接失败，提示会点名这一根因并给出可操作的建议——把启动器代理切换到「自动检测系统代理」，或在代理软件中开启 TUN / 关闭 Fake-IP DNS——而不是一句笼统的网络错误。

### Linux 支持（实验性）

- **「游戏还在运行」的检查真正生效**：以前 Linux 上这套检查实际认不出正在运行的游戏，更新、修复、卸载照样开始，最后撞在被占用的文件上留下残留；现在会正确拦下正在运行的游戏。
  - 改按启动器写入的会话标记识别由本启动器启动的游戏，也认得映射着安装目录内文件的外部启动（官方启动器、桌面脚本）；拦截时照样点名实际在跑的进程。

- **启动前检查兼容环境**：Linux 上启动游戏前先检查兼容前缀所在的环境，问题就地拦下并给出可操作的建议。
  - 前缀位置不可写、所在挂载点禁用了可执行（noexec）、文件系统不支持符号链接，各自说明原因与改法（例如改到支持符号链接的文件系统——ext4 上）。

- **资源面板能从兼容环境读取 UID**：Linux 上资源面板可以在配置的兼容前缀（UMU 或 Wine）里找到游戏 Cookie 并读出 UID，不再依赖手动填写。

- **双开与唤起修复**：同一登录会话内只允许一个实例，第二次启动改为唤起已有窗口。
  - 此前从不同启动环境（桌面快捷方式、终端等）再次启动会开出第二个实例；Linux 上第二次启动更是没有任何反应。

- **「系统」代理档读取 GNOME 的系统代理设置**：Linux 上「系统」代理档会读取 GNOME 的系统代理设置。

- **动画「跟随系统」档改为真实探测**：动画「跟随系统」档现在真实探测系统的动画开关，不再固定取默认值。

- **诊断信息更全**：导出的诊断信息新增以下内容，反馈 Linux 问题时不必再手动收集。
  - 运行器进程快照、运行器的输出（stdout/stderr）、GPU 与 Vulkan 探测、桌面会话类型、兼容前缀元数据与本机已安装的 Proton 构建清单。

- **新增 `.rpm` 安装包**：Fedora、RHEL、openSUSE 等使用 RPM 的发行版现在可以直接安装 `.rpm` 包，与 `.deb` 平行。
  - 安装布局与 `.deb` 一致（程序在 `/opt/cafe-launcher`），装好后从桌面菜单或终端运行 `cafe-launcher` 即可启动，桌面菜单项与图标一并装上。

---

### 技术更改

以下为本版在面向使用者的更新之外、实现层面发生的改动，可在发布页展开查看。

<details><summary>展开查看技术细节</summary>

- **自更新执行链（ADR-037）**：应用内更新拆成两段进程——`src/Cafe.Launcher.Updater` 作为自包含单文件 helper 复制到 `%TEMP%`，等主进程退出后再按安装版（静默运行安装器）与便携版（整目录替换）分别落地并重启启动器。
  - 信任锚是发布页的 `SHA256SUMS`：先校验 SHA-256 再执行；缺少摘要清单、对应资产或本机 helper（只有 win-x64 包携带）时降级为浏览器交付——绝不执行未校验的文件，也不会先下载一个注定装不上的包。可用性判定返回的是原因（平台 / 缺 helper / 无可校验包）而不是布尔，对话框据此分别说明。
  - helper 目录会在启动器下次启动时清理，成功路径补上完成日志。

- **更新源与接口**：检查更新改用主仓库 `bluearchive-cafe/Cafe.Launcher.Avalonia` 的 Releases 接口；地址未通过出站校验时按失败结果处理，不再把校验拒绝变成崩溃。

- **运行中闸门的进程识别（ADR-032 / ADR-036）**：进程名家族由游戏自带 config 的 `name` 与可执行文件 `params` 推导，因此能认出比宿主活得更久的反作弊兄弟进程；Linux 改按启动器写入的所有权标记识别，并把安装目录 `maps` 与配置的 Proton 构建纳入判据。
  - 破坏性路径在写入边界二次复检，进程扫描本身带时间上限；运行中进程的显示名统一由一处生成，不再重复 `.exe` 后缀。

- **会话看护与状态行（ADR-035）**：`GameSessionMonitor` 用同一套家族扫描看护会话；运行器在游戏进程出现前退出时带退出码汇报失败，并把主窗口从托盘恢复出来。

- **Unix 单实例（ADR-034）**：所有权判定改为数据根内的内核原子锁套接字（`Local\` 命名空间在 POSIX 会话间不生效，无法跨启动环境去重）；第二次启动的转发信号在 Unix 上走本地 Unix 域套接字。

- **网络与代理出站**：直连路径容忍 Fake-IP 模式的应答段（IPv4 `198.18.0.0/15`、IPv6 ULA `fc00::/7`，AUD-SEC-006 的 IPv6 延伸）；随后连接失败时在异常上打标记，让错误呈现能把根因归到 Fake-IP。出站仍只有 `RemoteHttpTransport` 一个出口，租约、SSRF 校验、手动重定向、缓冲与重试范围都留在它内部。

- **发布说明展示管线**：预览控件迁到 MarkView.Avalonia + Markdig（GFM、GitHub Alerts、任务列表），`ReleaseNotesMarkdownSanitizer` 只剥图片与链接目标、保留文字，`LinkClicked` 一律吞掉——发布说明无法触发外部导航或远程图片加载。

- **诊断采集**：系统信息新增 GPU 与 Vulkan 探测、桌面会话类型、兼容前缀元数据与本机 Proton 构建清单；日志导出加入运行器进程快照，并捕获运行器 stdout/stderr。

- **卸载与目录清理（ADR-030）**：递归删除不再依赖 `Directory.Delete(recursive: true)`（树内有 junction 时会失败），改为自行遍历、把链接按链接删除，并返回删不掉的条目（反作弊留下的 `Xigncode:{GUID}` 数据流条目）而不是抛异常；目录尺寸由 `DirectorySizeProbe` 在对话框打开后异步补齐。只读文件先清标记再删，不再让整次卸载中止。

- **UI 契约收敛**：间距与透明度令牌收尾并新增 Opacity 契约（零视觉变化）；失败措辞归入同一语族、向导错误图标并入 AlertCircle 族；设置项重新归组，日志查看器顶端间距移到固定工具行；golden 基线随 UI 重建。

- **工程与构建**：覆盖率棘轮基线随 Windows 实测下移，Linux 专属测试补上可见的平台门；CI 收敛 NuGet 缓存键（避免 `setup-dotnet` 的 `cache: true` 每次写入新缓存条目）、把 Release 测试移到覆盖率之后以免 RID 还原污染锁定还原，以压缩 Actions 存储用量。

- **Linux 安装包**：新增 RPM 打包路径，与已有的 `.deb` 平行——同一份自包含发布树交给 `rpmbuild` 打成 `Cafe.Launcher.Avalonia_<tag>_linux-x64.rpm`，预发布版本沿用 deb 的转换（`1.1.0-beta.11` → `1.1.0~beta.11`，`~` 保证排在正式版之前）；发布工作流用 `rpm --root` 装进临时根目录做启动冒烟，并把 RPM 一并计入 `SHA256SUMS` 与两处发布附件。

- **内部实现整理**：展示层重复装配、逐文件进度门控、临时目录所有权、时钟接缝、静态入口与日志登记所有权等重复逻辑各收一处，收敛为单一所有方；删除不可达的代码路径与未被消费的样式令牌；三个手写变更通知模型迁移到 `ObservableObject`。

</details>
