## v1.0.0

> [!NOTE]
> 本版本作为首个稳定版本，重点完善窗口覆盖层动画表现、下载状态同步机制以及后台任务生命周期管理，进一步提升应用运行稳定性。

### 修复

- **覆盖层动画表现**：修复对话框、设置页面、日志查看器、调试面板以及安装向导等窗口覆盖层在显示和关闭过程中的动画异常。现在遮罩层、面板移动效果以及内容透明度会分别进行独立过渡，避免动画效果相互干扰。

- **覆盖层动画时序**：优化覆盖层显示流程，移除不必要的动画等待时间，使背景遮罩、面板以及内容区域能够按照预期顺序完成过渡。

### 稳定性改进

- **下载状态同步**：完善后台下载任务与 UI 状态之间的同步机制，确保后台线程产生的状态变化能够正确更新界面，并避免过期状态覆盖最新操作结果。

- **资源生命周期管理**：修复窗口关闭、组件释放后仍可能接收到旧任务回调的问题，避免已释放对象继续更新界面或产生异常行为。

- **进度更新可靠性**：优化下载进度更新流程，确保后台任务提交的进度变化能够经过 UI 调度后正确刷新进度显示。

- **自动更新检查优化**：稳定版更新频道现在会正确跳过预发布版本，并能够识别 `1.0.0-beta.10` 到 `1.0.0` 的升级路径。同时继续验证发布文件路径、下载地址以及文件大小，确保更新过程可靠。
