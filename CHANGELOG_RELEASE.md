## v1.0.1-beta.1

> [!NOTE]
> 本版本重点重构主界面的远端内容（资讯）区域：横幅轮播改为悬停/聚焦自动暂停与浮动控件，新闻分类改用原生标签页，社交链接调整为右上角图标按钮组。同时移除了启动崩溃恢复流程与"详细"状态显示模式，并重组了项目结构。
>
> 本版本对界面、布局和项目结构进行了调整，为后续的设计系统重构奠定基础。

### 界面与交互

- **横幅轮播重构**：插入式水平滑动过渡替代原有淡入淡出；移除轮播暂停/播放按钮，轮播改为在鼠标悬停或键盘聚焦时自动暂停、离开后自动恢复；上一张/下一张按钮与指示圆点改为叠加在横幅上的浮动控件，仅在悬停或聚焦时显示。

- **新闻资讯改造**：分类标签由自定义按钮切换改为原生 TabControl（自定义主题样式，保留强调色指示条），选中分类通过双向绑定切换；新闻条目改为单行显示并省略超出文本，列表区域在固定最大高度内滚动，不再使用固定行高。

- **社交链接布局调整**：社交入口由内容区域内的整行胶囊按钮改为悬浮于资讯卡片右上角的竖排纯图标按钮组，悬停时显示名称提示。

- **内容卡片与滚动重构**：公告、横幅、新闻改为彼此独立的卡片，统一由外层滚动容器负责纵向滚动，并收紧卡片间距；公告文本超出部分改为省略显示。

- **资源面板图标**：更改了窗口标题区域资源面板的图标，使其贴合语义。

### 设置与行为

- **状态详情模式简化**：移除"详细"模式，仅保留"隐藏/紧凑"；默认值由"详细"改为"紧凑"。旧版设置文件中保存的 `detailed` 值会在读取时自动归一化为"紧凑"，无需手动调整。

- **设置页面移除顶部状态栏**：设置面板顶部不再显示视图标题、版本号、网络状态与磁盘空间概览。

- **移除崩溃恢复流程**：删除上次会话崩溃标记检测与启动时的"上次会话异常结束"对话框（查看日志 / 重置设置 / 继续）。异常仍会记录到本地日志，但启动时不再弹出恢复提示。

### 工程与开发

- **工作区重组**：应用项目移动至 `src/Cafe.Launcher.Avalonia/`，新增 `Cafe.Launcher.Avalonia.slnx` 解决方案统一管理应用与两个测试项目；同步更新构建、验证、分发与 CI 工作流。

- **调试体验**：Debug 构建自动附加 Avalonia 开发者工具。

- **测试覆盖**：更新并扩充 UI 契约与 Headless 测试（横幅交互、新闻标签页、社交按钮、设置覆盖层等）。
