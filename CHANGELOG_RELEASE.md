## v1.0.0-beta.2

本版本重点完善了启动器更新流程、网络代理一致性和设置状态管理，并对下载、界面及内部架构进行了系统性整理。

### 新增功能

- **更新文件选择** — 检查到新版本后，会显示该版本包含的全部发布文件。用户可查看文件名、下载地址和大小，并明确选择需要下载的文件。
- **更新服务代理** — 启动器更新检查改为通过专用服务获取 GitHub Release 数据，不再由客户端直接访问 GitHub Releases API。
- **更新通道支持** — 稳定版通道忽略预发布版本，测试版通道按语义版本规则比较正式版及预发布版本。
- **设置值规范化** — 加载和保存设置时会校验语言、主题、代理、下载源、限速、背景、更新通道等值，并修复无效颜色、调色板索引和旧版启动检查配置。

### 修复与改进

- **代理行为一致性** — 启动器配置、清单、资源面板、更新检查及下载相关请求现在均按当前请求显式使用直连或系统代理，避免共享代理状态造成请求间相互影响。
- **设置修改检测** — 设置对象的直接属性修改会立即标记为未保存；放弃修改时会恢复完整快照，避免漏报或错误保留部分改动。
- **下载恢复状态** — 下载任务状态的读取、保存和清理由下载服务统一管理；恢复任务时会核对游戏路径、版本、清单依据和下载源。
- **更新数据校验** — 发布文件必须包含非空文件名、有效的 HTTP/HTTPS 地址和正数文件大小；无效响应会终止本次检查并写入本地诊断日志。
- **更新错误诊断** — HTTP 请求失败、响应 JSON 无效和请求超时现在会记录明确的本地诊断信息。
- **外部链接安全** — 仅允许打开 `http`、`https` 和 `mailto` 链接，阻止文件、命令等非预期协议。
- **迁移向导一致性** — 旧启动器迁移向导复用统一的设置编辑器和选项数据，游戏路径、代理模式及关闭行为使用与设置页相同的值模型。
- **图片加载整理** — 移除独立的 URL 图片转换下载路径，统一使用现有图片缓存与视图模型数据流。
- **自定义壁纸恢复** — 设置页、背景模块和通知模块统一使用同一份设置状态，修复选择自定义图片或文件夹后背景仍回退到内置壁纸的问题。
- **游戏修复恢复** — 修复安装完成后执行游戏修复时，操作状态写入非界面实例而导致看似无响应的问题。
- **修复期间保存设置** — 保存设置时不再重复恢复并取消正在运行的修复任务，避免连续出现“Operation stopped.”通知；用户主动停止操作改为警告提示。

### 界面优化

- **更新选择对话框** — 新增卡片式文件列表、文件大小标识以及清晰的悬停和选中边框；选中状态保持稳定的背景和文字颜色。
- **操作语义精简** — 下载按钮在未选择文件时保持禁用；移除重复选择提示，并使用时钟图标表示“稍后”操作。
- **设置面板拆分** — 设置选项和外观状态分别由独立视图模型管理，主题色、壁纸调色板、背景填充及条件控件状态更加稳定。
- **通知主题适配** — Toast 图标颜色通过主题资源转换器解析，通知数据不再直接依赖 Avalonia 画刷对象。
- **Toast 卡片分隔** — 移除会覆盖相邻通知的逐项阴影，改用主题边框保持多条通知之间的清晰层次。
- **游戏操作本地化** — 启动校验、安装、更新、修复、卸载、进度阶段及错误提示统一使用英语、简体中文和日语本地化文案。
- **官网链接跟随下载源** — 官方下载源打开 `bluearchive.jp`，Cafe 下载源打开 `bluearchive.cafe`。

### 技术变更（面向开发者）

- 将常量拆分为 API 配置、构建信息、游戏路径和通用启动器常量，移除客户端中的 GitHub 访问令牌。
- 引入统一的代理感知 HTTP 客户端租约接口，生产和测试请求共用同一调用模型。
- `LauncherSettings` 改为可观察模型；新增 `SettingsNormalizer`、`SettingsOptionsViewModel` 和 `SettingsAppearanceViewModel`。
- 设置编辑器及相关设置视图模型改为共享生命周期，确保单窗口中的设置消费者读取同一状态。
- 主窗口外壳、远程内容和游戏操作视图模型改为共享生命周期，确保单窗口中的状态与命令使用同一对象实例。
- 将对话框状态注册为单例，确保设置页与主窗口共享同一更新对话框实例。
- 将下载状态持久化逻辑并入下载服务，移除单独的 `DownloadStateService`。
- 新增更新对话框、设置规范化、Toast 数据纯度、HTTP 客户端及 UI 样式契约测试。
