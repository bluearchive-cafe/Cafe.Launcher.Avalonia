## v1.1.0-beta.8

![Cafe Launcher v1.1.0-beta.8 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.8/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.8-release-banner.png)

> [!NOTE]
> 本版本聚焦「崩溃之后看得见、失败之后说得清」：启动器意外退出时会弹出崩溃报告窗口，带崩溃编号、可复制的技术细节和「打开日志目录」入口；启动游戏失败时会说明具体是哪个运行环境出了问题，不再只给一句通用提示；内置壁纸不再拖慢启动；macOS 的支持范围也已写明——只能安装、更新和修复游戏，暂不支持启动游戏。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.7`，这些向后兼容的缺陷修复与体验改进将预发布号递增为 `v1.1.0-beta.8`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

### 崩溃诊断

- **崩溃后会弹出报告窗口**：以前启动器崩溃后窗口直接消失，只能自己猜发生了什么。现在无论问题出现在启动过程中还是界面运行中，都会弹出一个崩溃报告窗口：显示崩溃编号、发生时间、版本与构建信息，技术细节默认折叠，可以一键复制，也可以直接打开日志目录。窗口只有三个按钮——「打开日志目录 / 复制详情 / 退出启动器」。同一次运行只把第一个崩溃作为主报告，之后的问题只追加日志；关闭窗口后启动器随即退出，不会回到主界面。

- **崩溃报告只收集必要信息**：报告里只有异常内容、应用版本、构建信息、操作系统与界面语言，路径中的用户目录会替换成 `%USERPROFILE%`，不会带上你的用户名。报告保存在启动器的数据目录，最多保留 10 份或 30 天；数据目录写不进去时会改用临时目录，两处都写不进去时报告窗口仍会打开并说明情况。隐私说明（PRIVACY.md）已同步列出这一项。

### 启动与游戏管理

- **启动失败会说明原因**：以前「没有可用运行环境」只会提示一句「游戏进程未能启动」，真正的原因（当前平台不支持 / 运行环境缺失 / 运行环境损坏）只写进日志，用户看不出为什么。现在提示会直接写明是哪个运行环境、为什么不能用，例如「没有可用的运行环境。Windows（原生）：当前平台不支持。」

- **启动更快，不再被内置壁纸拖慢**：内置壁纸（2560×1388）以前在界面出现之前就被同步解码，随后第一次刷新还会再解一遍。现在启动阶段不再解码，壁纸改在后台加载，加载完成前窗口先显示主题底色——同一张壁纸在启动时只解码一次。

### 设置

- **「重置设置」的确认框现在按 Escape 会取消确认**：这个确认框以前不在弹窗层级里，按 Escape 会关掉下层的设置页，而不是取消确认。现在它和其他弹窗一致：Escape 取消确认，也不会再穿透到下层界面。

### 平台支持

- **macOS 目前不支持启动游戏**：macOS 版本可以安装、更新和修复游戏；启动游戏需要额外的兼容运行层，当前不提供，也暂无支持计划。README 与文档站已同步说明。

### 安全与可靠性

- **游戏文件清单的路径校验更严格**：清单里如果出现指向游戏目录本身（而不是具体文件）的条目，以前会让下载的临时文件落到游戏目录之外，随后安装阶段报错收尾；现在这类条目会被直接拒绝。

- **网络响应体加上大小上限**：从服务器读取数据时，响应体超过 64MiB 会直接中止，异常的服务器响应不会再无限占用内存。

- **部分静默失败改为记入日志**：记录安装来源、读取资源面板的 Cookie 数据等后台小故障以前会被静默忽略，现在会写进诊断日志，便于反馈问题时定位。
