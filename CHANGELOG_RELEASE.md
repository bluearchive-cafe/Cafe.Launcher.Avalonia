## v1.0.0-beta.9

本版本完成启动器原生本地化迁移，强化自身更新的发现与失败恢复体验，升级安装/更新失败时的通知操作，并进一步改善 Toast、模态窗口和远端内容的交互可靠性。同时整理下载流程与窗口协调层，更新桌面 UI 依赖与构建 SDK。

### 新增

- **原生多语言资源**：界面文本改由 `.resx` 与 .NET `ResourceManager` 提供，继续支持自动、英语、简体中文、繁体中文和日语。手动切换语言会同步日期、数字与货币格式；切回“自动”会恢复启动时的系统语言与格式设置。

- **启动时检查启动器更新**：启动器初始化完成后会在后台检查所选更新频道；检测到新版本时会以通知提示。现在可在“通用”设置中关闭此检查，默认开启。

- **可恢复的安装与更新失败通知**：对于可重试的安装或更新失败，通知卡片会提供“重试”和“查看日志”操作，无需重新走完整操作流程即可继续排查或重试。

- **调试面板（Debug 构建）**：标题栏新增调试入口，可查看版本、提交、运行时、系统与日志路径信息；动态调整日志级别、写入测试日志、测试各类 Toast 与错误处理，并可查看当前下载状态、暂停/继续/停止下载、刷新状态、查看或重置设置、导出日志及打开数据目录。严重错误提示支持查看日志、复制完整错误详情，以及关闭提示后继续运行。

### 移除

- **移除 Toast 通知开关**：移除“通用”设置中的“通知设置 / Toast 通知”开关及 `toastNotificationsEnabled` 配置字段。Toast 通知现在始终启用；原位置改为“启动时检查更新”开关。已有配置中的旧字段不会继续写回。

### 优化

- **语言切换与资源校验**：已打开的设置、首次向导和对话框会随语言选择立即刷新。新增资源键、格式化参数、卫星资源与 Release 产物契约检查，避免缺失翻译或格式错误进入发布版本。

- **Toast 通知体验**：通知卡片支持标题、操作按钮、自动关闭进度条和操作执行状态；最新通知会置顶，多条通知重排时会平滑过渡，入场与退出动效统一使用新的动效令牌。

- **错误信息与诊断链路**：统一处理启动、设置、安装、修复、资源面板等流程中的异常，保留异常链并写入诊断日志，让用户看到的提示和可供排查的日志信息更一致。

- **远端内容与模态交互**：远端内容加载失败时显示明确的失败状态，Banner 轮播的淡入过渡遵循动效偏好；设置、资源面板、日志、调试面板和确认对话框使用统一的模态栈管理，顶层窗口打开时会阻止下层控件继续响应点击，Esc 也只会交由最上层窗口处理。

- **磁盘空间检测性能**：可用空间查询按磁盘根目录缓存 30 秒，减少界面刷新与设置编辑时的重复磁盘访问。

### 修复

- **更新检查回退**：当常规更新检查路径不可用时，启动器会使用备用路径继续查询可用更新，降低暂时性服务异常导致无法发现新版本的概率。

- **本地化资源失败提示**：资源键或格式化异常会进入应用级错误处理并记录诊断；用户只会看到安全提示，不会暴露资源键或格式模板。

- **显示检查更新失败详情**：手动检查启动器更新失败时，通知会显示 HTTP 请求、超时或 JSON 解析等具体异常原因，而不再只显示笼统的失败提示。

- **受保护目录卸载拦截**：卸载流程的系统路径保护逻辑与官方启动器行为对齐，避免将磁盘根目录、用户目录或系统目录误作为可删除的游戏目录。

- **对话框与主界面布局细节**：修复对话框按钮的边缘模糊问题，调整遮罩透明度，并修正进度面板和状态面板的行列定位，确保不同状态下的内容布局稳定。

### 架构与依赖

- **Shell 运行时与本地化模块加深**：移除已被 Shell 生命周期替代的协调器；窗口仅依赖精简的 Shell 运行时接口，游戏操作旅程由工厂组装。本地化界面绑定改为按资源键查找，删除数百个与资源键一一对应的转发属性；切换语言时所有绑定会即时刷新。

- **审计与规范修正**：完善错误处理、设置规范化、重试策略、日志标题、公开 API 文档及本地化测试约束。

- **下载流程模块化**：将清单差异计算、并发下载执行、校验安装和下载计划拆分为独立组件；安装/更新会结合远端清单、本地清单及文件状态构建下载计划，便于维护下载、校验、重试和断点续传逻辑。

- **窗口协调层整理**：将启动、安装、修复、卸载、重试与刷新收敛至游戏操作旅程，并由 Shell 生命周期集中管理覆盖层显示、Esc 行为、状态详情显示和跨功能事件编排。

- **测试与开发环境隔离**：测试进程使用独立的启动器用户数据目录，避免读写真实的本地设置；VS Code 工作区会排除 `.worktrees`，减少 C# Dev Kit 扫描过期项目造成的干扰。

- **依赖更新**：Avalonia 及 Fluent、ColorPicker、Desktop 组件升级至 `12.1.1`，Microsoft.Extensions.DependencyInjection 升至 `10.0.10`，Serilog 升至 `4.4.0`；构建环境固定为 .NET SDK `10.0.302`。
