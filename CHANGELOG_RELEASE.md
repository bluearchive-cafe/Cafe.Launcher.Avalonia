## v1.1.0-beta.10

![Cafe Launcher v1.1.0-beta.10 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.10/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.10-release-banner.png)

> [!NOTE]
> 本版本聚焦「卸载得干净、拒绝得明白、界面更顺手」：卸载可以连整个安装目录一起清除，标准卸载也会顺手删掉桌面快捷方式，删不掉的东西与主动保留的 Prefix 都会一件件说清楚；凡是以前点了没反应的地方——游戏还在跑、游戏目录被删或被移走、确认之后状态又变了——现在都会就地说明原因；设置里新增「游戏启动后的行为」，保持窗口、最小化到托盘还是直接退出由你决定；资源面板刷新不再清空列表；系统代理（含 PAC 脚本）与需要认证的域内代理都能正常工作；发布页新增摘要清单与构建来源证明，下载后可自行校验。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.9`，这些向后兼容的新特性与缺陷修复将预发布号递增为 `v1.1.0-beta.10`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。macOS 仅支持安装、更新和修复游戏，暂不支持启动游戏。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

### 设置

- **游戏启动后的行为可以自己定**：以前游戏启动成功后启动器固定最小化到托盘，不想被最小化的人只能每次自己把窗口找回来。设置「常规」的「应用偏好」分组新增「游戏启动后的行为」（默认「最小化到托盘」），可选「保持窗口打开」或「退出启动器」，改完立刻生效，三个选项各自的提示也如实对应——「游戏正在启动。」「游戏正在启动 — 启动器已最小化到托盘。」「游戏正在启动 — 启动器即将退出。」选「退出启动器」时启动器会真的退出（托盘图标一并消失），想留在托盘里就选「最小化到托盘」。

### 游戏卸载

- **可以连整个安装目录一起清除**：卸载确认框新增一项默认不勾的「彻底清除」。勾上后，除了清单里的文件与两个状态文件，连整个安装目录（含清单外的残留与已经废弃的暂存文件）和启动器托管的兼容环境目录（Prefix）一起删除；勾选项上直接给出这两处的实测大小，打开确认框时先显示「正在统计大小…」，测好之后把数字填上。不勾则维持原有的标准卸载范围。

- **标准卸载也会删掉桌面快捷方式**：以前卸载只删游戏清单里的文件与两个状态文件，桌面上的快捷方式留在原地，重装之后也不会自动恢复。现在卸载成功时会一并删除；桌面上本来就没有、或当前平台不支持创建快捷方式，都不算失败。

- **卸载的结果与失败都会说清楚**：完成提示会列出删不掉的项目——游戏的反作弊会在安装目录里留下几个用户态删不掉的条目（形如 `Xigncode:{GUID}`，重启后通常即可删除）——以及因为不在启动器管理的目录内而被保留的自定义 Prefix。卸载前的检查不通过时会说明具体原因（游戏目录被删掉或移走了、设置里把路径指到受保护的目录、安装信息不完整），而不是点了卸载没有任何反应；删除目标被启动器的路径检查拒绝时，也会给出理由和目标路径，不再把整句英文原文抛给用户。

- **确认之后才被拒绝的操作也会说明**：修复与卸载的确认框可能开着好一会儿（要统计目录大小，也可能你刚好离开），这期间后台刷新若把游戏状态改掉（例如从「可修复」变成「有更新」），以前点过确认也不会发生任何事。现在会明确提示当前状态不允许该操作，而不是把确认过的手势白吃一次。

### 游戏运行检测

- **游戏还在跑时不再放行**：判断「游戏是否在运行」以前只认启动器自己启动的那个进程，而它往往在游戏起来之后就退出了，真正占着安装目录的是反作弊的同族进程——于是游戏还在运行时卸载、安装照样开始，删除撞在被占用的文件上，最后留下一层删不掉的残留。现在按进程名字家族识别（名字取自游戏自己带来的配置，不写死在启动器里），提示里点名实际在跑的那几个进程，例如「游戏正在运行：xldr_BlueArchiveOnline_JP.exe / BlueArchive.exe。请关闭游戏后再修改文件。」

- **动文件之前会再查一次**：确认框可能开着一会儿，下载可能要几分钟，那期间从桌面快捷方式把游戏启动起来，先前的检查就已经过期。卸载在真正删除之前、安装/更新/修复在替换安装目录里的文件之前都会再确认一次；被拦住的是那一次写入而不是整个流程，已经下载好的部分留在盘上，关掉游戏后重试会从已有字节继续。

### 资源面板

- **刷新不再清空面板**：刷新、重载时保留已经加载出来的版本信息，只有换了 UID 才把旧数据作废，面板不再闪空白。「保存」按钮改为按有无改动启停——没有改动时不可点，保存成功后立刻回到不可点。

- **状态条与版本行更清爽**：状态条不再把 UID 重复念一遍，出错时用危险色与普通提示区分开；官方版本与本地化（汉化）版本一致时，把两列相同的内容折成一行结论「官方与本地化（汉化）资源版本一致」加版本号；「修改 UID」入口常驻，不再等来源切到自定义才出现。

- **资源面板里设置的 UID 不再被设置页悄悄改回旧值**：手动 UID 由资源面板写入后，设置页里那份草稿仍是旧值，于是你在设置页改动任意一项并保存时，整份草稿写回会把 UID 悄悄回滚——而设置页里没有这个字段的入口，出了问题也无处发现。现在每次保存后，磁盘上、设置页草稿里和应用正在用的都是同一个值。

### 界面与外观

- **应用图标换新**：窗口与任务栏的图标、Windows 可执行文件与安装向导，以及 macOS 与 Linux 的打包图标，都换成了新图。

- **官网与社交入口按钮的悬停、按下不再透出壁纸**：主界面右侧那几个直接浮在壁纸上的图标按钮，鼠标悬停或按下时状态层是半透明的，底下的壁纸会透出来；现在改用不透明的专用配色，明暗主题各自取色。

- **窗口出现时就是壁纸**：内置壁纸曾经为了缩短启动改为后台加载，代价是窗口出现后先显示一小段主题底色；现在启动阶段就把壁纸准备好，第一帧即为壁纸，代价是启动阶段多一次内置壁纸解码。

### 网络与下载

- **系统代理的取用方式对齐 Windows 自身**：以前读系统代理设置时忽略自动配置脚本（PAC），PAC 变更也要重启启动器才生效。现在即使「使用代理服务器」是关的，PAC 脚本也会被采用，脚本一改就立即换用新代理；「系统」模式下需要认证的域内代理也能正常工作，不再每个请求都失败。

- **安装、更新与下载更省资源**：安装/更新阶段对每个文件做 CRC-64 校验，以前一次只读一个文件，现在按机器核心数有界并行（最多 8 路），大清单的等待明显缩短；下载重试时不再每次重新申请 256 KiB 缓冲。

- **进度不再往回跳**：校验、清单比对与卸载的进度以前可能在并行阶段来回跳，重复报走过的百分比、甚至瞬时回退；现在同一阶段内只向前。

- **暂停、继续、停止点击立即响应**：这三个按钮的处理以前要等日志写完，日志一忙，点击就被拖住。

### 安全与隐私

- **跟随跳转时不再把凭据发给别的站点**：请求被重定向到另一个主机（域名或端口不同）时会剥掉授权头，与浏览器和 .NET 的既有约定一致；同一主机内的跳转不受影响。

- **发布产物可以自行校验**：发布页新增 `SHA256SUMS` 摘要清单，六个分发包（Windows 压缩包与安装程序、macOS 压缩包、Linux 压缩包/AppImage/deb）都在其中，可用 `sha256sum -c` 核对下载到的文件有没有被截断或篡改；每个分发包与这份清单还附有构建来源证明，可在 GitHub 的公共证明账本上核验它们确实由本仓库在对应版本上构建，而不只是「没有被改过」。
