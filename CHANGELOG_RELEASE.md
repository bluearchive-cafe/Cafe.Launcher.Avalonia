## v1.1.0-beta.11

![Cafe Launcher v1.1.0-beta.11 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.11/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.11-release-banner.png)

> [!NOTE]
> 本版本聚焦「更新自己、看住会话、补齐 Linux」：Windows 上启动器可以在应用内下载并安装新版本——发布说明直接预览、更新包先经 SHA-256 校验、点一下「重启以更新」即可完成；游戏启动后，控制面板与托盘都能看到会话状态（启动中 / 运行中 / 已退出 / 未能启动），第三方运行器秒退也能第一时间恢复窗口并报告；Linux 实验构建的单实例、运行中保护、兼容环境预检、UID 读取与诊断信息全面补强；同时适配了 Fake-IP 模式的代理软件，托盘菜单新增「开始游戏」与「设置」入口。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.10`，这些向后兼容的新特性与缺陷修复将预发布号递增为 `v1.1.0-beta.11`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。macOS 仅支持安装、更新和修复游戏，暂不支持启动游戏。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

### 启动器更新

- **Windows 上可以直接在启动器内更新**：检查到新版本时，「发现新版本」对话框内直接显示该版本的发布说明；点「下载」在启动器内完成下载并显示进度，完成后提示「更新已就绪，重启以应用。」，再点「重启以更新」，启动器退出、自动完成安装并重新打开，全程不需要手动下载或运行安装程序。安装版与便携版都支持。下载的更新包先按发布页的 SHA-256 摘要清单校验，校验不通过不会安装；发布会缺少摘要清单或本机所需的安装包时，同样改为打开发布页，绝不执行未经校验的文件。

- **不能在应用内更新的平台直接前往发布页**：macOS 与 Linux 上，「发现新版本」对话框的主按钮是「前往发布页」，点击即用浏览器打开发布页，下载安装包由你手动完成——原先先从列表里挑文件再下载的间接方式已移除。

- **检查更新失败不再闪退**：更新源地址未通过安全校验时，现在提示「无法检查 Cafe Launcher 更新。」，不再直接崩溃退出。

### 启动与会话

- **游戏启动后的状态看得见了**：以前点了开始游戏，提示「游戏正在启动」之后便无从知晓游戏有没有真的起来。现在控制面板新增一条会话状态行——「游戏启动中」「游戏运行中」，游戏结束后换成「游戏已退出（退出码 X）」，正常退出不打扰、只换状态行。使用第三方运行器（Wine/Proton）启动时，如果运行器在游戏进程出现之前就退出（例如前缀损坏、缺少依赖），启动器会先把主窗口从托盘恢复出来，再提示「游戏未能启动（退出码 X）」，不再留一个没有下文的「游戏正在启动」。没有会话时状态行整行隐藏。

- **启动校验拿不到远程清单时会说明**：启动校验选择远程文件清单但获取失败时，状态行显示「无法获取远程文件清单，已跳过本次启动校验。」，不再无声跳过。

- **不再卡在「正在生成文件列表」**：判断游戏是否正在运行时的进程扫描加了时间上限，扫不出来时改为提示「无法确认游戏是否正在运行。请关闭游戏后重试。」，等待期间的取消也立即生效，不再一直转圈且无法取消。

### Linux 支持（实验性）

- **「游戏还在运行」的检查真正生效**：以前 Linux 上这套检查实际认不出正在运行的游戏，更新、修复、卸载照样开始，最后撞在被占用的文件上留下残留。现在改按启动器写入的会话标记识别由本启动器启动的游戏，也认得映射着安装目录内文件的外部启动（官方启动器、桌面脚本）；拦截时照样点名实际在跑的进程。

- **启动前检查兼容环境**：Linux 上启动游戏前先检查兼容前缀所在的环境，问题就地拦下并给出可操作的建议：前缀位置不可写、所在挂载点禁用了可执行（noexec）、文件系统不支持符号链接，各自说明原因与改法（例如改到支持符号链接的文件系统——ext4 上）。

- **资源面板能从兼容环境读取 UID**：Linux 上资源面板可以在配置的兼容前缀（UMU 或 Wine）里找到游戏 Cookie 并读出 UID，不再依赖手动填写。

- **双开与唤起修复**：以前从不同启动环境（桌面快捷方式、终端等）再次启动会开出第二个实例；现在同一登录会话内只允许一个实例，第二次启动改为唤起已有窗口（此前 Linux 上第二次启动没有任何反应）。

- **「系统」代理与「跟随系统」动画**：Linux 上「系统」代理档会读取 GNOME 的系统代理设置；动画「跟随系统」档改为真实探测系统的动画开关，不再固定取默认值。

- **诊断信息更全**：导出的诊断信息新增运行器进程快照、运行器的输出（stdout/stderr）、GPU 与 Vulkan 探测、桌面会话类型、兼容前缀元数据与本机已安装的 Proton 构建清单，反馈 Linux 问题时不必再手动收集这些。

### 托盘

- **托盘菜单新增「开始游戏」与「设置」**：右键托盘图标可以直接开始游戏或打开设置，不必先打开主窗口；游戏启动中或运行中时，「开始游戏」一项会显示当前会话状态（「游戏启动中」「游戏运行中」）。两处入口在需要时会先把主窗口恢复出来，让确认框与提示看得见。

- **从托盘恢复不再重复提示**：通过托盘恢复窗口时不再重走一遍带启动参数的流程，「游戏正在启动」提示不会重复弹出；退出启动器时也不再把托盘图标释放的信号误记为一次崩溃。

### 界面与交互

- **失败就地重试**：公告与远程内容加载失败时，失败卡片上直接提供「重试」按钮，不必重新进入页面；失败卡片补上了底色与圆角，不再透出壁纸。

- **失败提示带「查看日志」**：安装、更新等操作以失败告终时，提示上直接提供「查看日志」入口。

- **网络错误提示更友好**：网络请求失败时提示「网络请求失败，请检查网络或代理设置后重试。」，原始错误信息只进日志与诊断导出，不再整句抛给用户。

- **提示条悬停暂停**：提示条的停留时长改为按类型固定的档位，鼠标悬停时暂停计时，读完再消失，长提示不再读一半就没了。

- **底部显示下载源**：主界面底部状态区显示当前使用的下载源（「下载源：××」），加载中也会标出。

- **UID 生成提示**：资源面板在 UID 尚未生成时显示一条可关闭的提示——「通过 Cafe 源首次启动游戏后，才会生成 UID。」

- **设置页重排与「打开游戏目录」**：游戏设置页的路径旁新增「打开游戏目录」按钮；设置项重新归组——更新通道并入常规页「更新」分组，状态面板与远程内容卡片移入常规页「显示」分组，运行器/前缀/Proton 路径并入游戏页「兼容运行环境」分组，外观页只留主题与背景。

- **图标与细节**：应用图标进一步调整；设置向导「跳过」按钮与路径选择按钮换用更贴切的图标；日志查看器顶端间距修正，与过滤栏的间距不再随内容滚动；「游戏正在运行」提示里的进程名不再重复一遍 `.exe` 后缀。

### 网络与代理

- **适配 Fake-IP 模式的代理软件**：使用 Clash、mihomo 等以 Fake-IP 模式接管 DNS 的代理软件时，直连路径上解析结果落在 Fake-IP 保留段（IPv4 198.18.0.0/15、IPv6 fc00::/7）不再被当作异常拦截；若随后仍连接失败，提示会点名这一根因并给出可操作的建议——把启动器代理切换到「自动检测系统代理」，或在代理软件中开启 TUN / 关闭 Fake-IP DNS——而不是一句笼统的网络错误。
