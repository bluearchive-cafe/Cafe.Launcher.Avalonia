## v1.1.0

![Cafe Launcher v1.1.0 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0/docs/assets/release-banners/cafe-launcher-v1.1.0-release-banner.png)

自上个稳定版 `v1.0.0` 以来的一个多月里，Cafe Launcher 迎来了第二个正式版。我们重塑了整个界面，把游戏的安装、更新、修复与卸载做得更透明，补上了 Windows 应用内更新，并首次带来 macOS 与 Linux 实验构建。愿这一版让你少操一点心，多留一点时间给游戏。

感谢各位一直以来的支持。如果你在使用中遇到问题，欢迎通过 Issue 反馈；愿各位节日愉快，游戏顺利。

> [!NOTE]
> 这是 Cafe Launcher `1.1.0` 的稳定版。下文先列出自上一版 `1.1.0-beta.11` 以来的变化，再汇总自 `1.0.0` 以来整个 `1.1.0` 版本的变化；第二部分包含第一部分的成果。
>
> Windows 为正式支持平台；macOS 与 Linux 仍提供实验性构建。macOS 支持安装、更新和修复游戏，暂不支持启动游戏。Linux 可通过兼容运行环境启动游戏，实际效果仍取决于发行版、显卡驱动、Proton 版本及反作弊兼容性。
>
> 从 `1.1.0-beta.N` 升级到本版无需先卸载：更新通道选「稳定版」时，预发布版本会被正确识别为可升级到 `1.1.0`。

> [!WARNING]
> 作为 `1.1.0` 的首个稳定版，仍可能存在尚未覆盖的问题。建议先备份游戏目录与设置；如遇崩溃、启动失败或游戏文件异常，可从启动器导出诊断日志并反馈。`v1.0.0` 仅提供 Windows 安装程序与免安装压缩包，macOS 与 Linux 支持是 `1.1.0` 首次加入的实验性功能：请在可回退的环境中试用，兼容运行环境在组件大版本更新后建议重新验证一次游戏启动。

### 自上一版 v1.1.0-beta.11 以来

- **资源面板重新设计**：UID 单独成区，可直接查看、编辑或手动输入，来源在「自动获取」与「自定义」之间切换；汉化资源项合并为紧凑列表，并标出「可用」或「待维护」。官方与汉化资源版本一致时合并为「官方与汉化资源一致」一行。「保存设置」只在确有改动时可点；尚未生成 UID 时，会持续说明「通过 Cafe 下载源首次启动游戏后，才会生成 UID。」。
- **首次设置的确认页更易核对**：「确认您的设置」页面改用完成徽章加带图标的摘要行，每一项都可以直接返回修改。
- **长路径更易辨认**：设置页与向导中的游戏路径会保留开头与末尾目录，只省略中间部分，一眼能看出实际目标。
- **应用内更新的不可用原因更明确**：当前平台不支持应用内更新、此安装缺少更新组件、该版本没有可供校验的安装包，三种情况会在「发现新版本」对话框里分别说明，并提供「前往发布页」；更新检查还会核对版本信息、发布标签与安装包名称是否对应，避免把旧版本安装包误认成新版本。便携版更新后如果新版本未能启动，更新程序会尝试恢复并重新打开旧版本。
- **对话框操作更稳妥**：确认与取消的排列、默认焦点和按钮外观已统一。普通确认可用 Enter 完成、按 Esc 取消；卸载等破坏性操作不会因按 Enter 被误触发，键盘焦点提示只在键盘导航时出现。
- **Linux 打包更完整**：新增面向 Arch 用户的 PKGBUILD 打包配方；`.deb`、`.rpm` 与 AppImage 的桌面入口补充了简体中文、繁体中文与日文名称、说明和搜索关键词，在系统应用菜单里更容易找到。
- **下载源与汉化资源的说明更一致**：设置和资源面板中的相关用词已统一，便于分辨游戏下载源与汉化资源各自的状态。
- **切换语言后法律信息正常显示**：设置「关于」里的版权说明、隐私政策与默认壁纸画师链接，在切换界面语言之后仍会留在原处可用。

### 自 v1.0.0 以来的完整变化

#### 更新启动器

- **Windows 安装程序改版**：安装程序改用 Inno Setup，提供简体中文、日文与英文安装界面；升级安装会自动沿用原有安装目录并安全接管旧版安装，选择安装位置时改用系统文件夹浏览对话框，卸载时还可选择是否删除应用数据。
- **Windows 可在应用内完成更新**：发现新版本后，可以在「发现新版本」对话框预览发布说明、下载并校验更新包，然后选择「重启以更新」。安装版与便携版均受支持，下载进度和速度会显示在对话框中。
- **发布说明可以在对话框里直接读完**：该版本的发布说明按完整排版呈现，标题、列表、链接、提示块与任务列表都能正常显示；启动时检查到新版本的提示条上也有「查看更新」，点它即可打开。
- **更新失败时有明确去处**：下载或安装失败时可直接「前往发布页」；检查更新的线路超时或不可用时，也会自动改用 GitHub 发布接口兜底。当前平台不支持应用内更新、此安装缺少更新组件，或该版本没有可供校验的安装包时，对话框会分别说明原因，再引导到发布页。便携版更新后如果新版本无法启动，会尝试恢复旧版本。
- **发布包可自行核对**：发布页提供 `SHA256SUMS` 摘要清单，可用于校验下载到的安装包与压缩包，并附有构建来源证明。启动器也会在应用内更新前核对摘要及对应版本，不会运行校验不通过的更新包。

#### 游戏安装、启动与卸载

- **游戏运行状态更清楚**：控制面板和托盘会显示「游戏启动中」「游戏运行中」或退出状态。第三方运行器在游戏出现前就退出时，启动器会恢复窗口并说明失败原因；没有可用的运行环境时，会逐项列出各环境及其不可用原因；启动校验未能取得远程文件清单时，也会明确告知本次已跳过校验。
- **游戏运行中保护覆盖更多情况**：安装、更新、修复和卸载会识别仍在运行的游戏及相关进程（包括随游戏一起出现、真正占用安装目录的反作弊进程），并在实际修改文件前再次检查，避免确认操作或下载期间游戏启动后继续改动安装目录。这项检查限时完成：暂时无法确认游戏状态时会明确说明，操作可以随时停止；确认之后情况发生变化时，也会明确提示原因，而不是无声跳过。Linux 上也会检查由启动器或其他入口启动、仍占用安装目录的游戏。
- **损坏文件更容易修复**：启动校验发现文件损坏时可直接进入修复；安装、更新和修复失败时可从提示打开日志。文件清单整理与校验过程中的进度更稳定，清理旧文件时按「停止」也能及时停下。下载遇到长时间无响应的连接会自动重试续传，不再无限等待；开始下载前会检查目录写入权限，覆盖文件前会清除只读属性；游戏安装在只读位置时，更新检查会安静通过。下载与校验整体更快，长操作中按钮保持可点；Cafe 下载源的备用线路地址也已修正。
- **卸载范围可以选择**：标准卸载会移除游戏文件、安装状态和桌面快捷方式；选择「彻底清除」还会清理整个游戏安装目录及启动器管理的兼容环境。确认框会补充待清理目录的大小；无法删除的项目和被保留的自定义 Prefix 都会在结果中说明。卸载遇到只读文件时不再整体失败。
- **游戏启动后的窗口行为可设置**：可以选择保持窗口打开、最小化到托盘或退出启动器。托盘菜单也新增「开始游戏」和「设置」入口。

#### 界面与日常使用

- **界面与主题全面更新**：主窗口、设置、对话框、通知和首次设置向导采用统一的视觉样式；动态配色、壁纸取色与明暗主题可即时预览，Windows 更改系统强调色或深浅色模式时也会立即跟随；应用图标也已换新。动效会尊重系统或应用内的减少动态效果设置。窗口出现的第一帧就是壁纸，不再先闪过一段底色；主界面右侧浮在壁纸上的入口按钮，悬停与按下也不再透出壁纸。
- **默认壁纸焕新**：默认背景更换为画师 めるき（Meruki）的插画《初めてのゲーム》，作品来源已在「关于」分区注明；壁纸与横幅会按窗口实际尺寸解码，超大图片同样保持清晰、不拖慢启动器。
- **首次设置更容易核对**：向导会在选择游戏目录时检查可写性并给出权限指引，确认页以摘要行展示选项，并可直接返回修改。设置页与确认页的长路径会保留开头和末尾目录，省略中间部分，便于辨认实际目标。
- **对话框操作更稳妥**：确认与取消的位置、默认焦点与按钮外观已统一。普通确认可用 Enter 完成、按 Esc 取消；卸载等破坏性操作不会因为 Enter 被误触发，键盘焦点提示只在键盘导航时显示。
- **资源面板更易浏览**：UID、UID 来源、资源版本与状态改为更紧凑的分组；刷新时保留已有内容，只有实际修改后「保存设置」才可用。尚未生成 UID 时会持续显示说明，Linux 还可从所选兼容环境读取 UID。
- **常用入口更顺手**：设置中新增「打开游戏目录」，随时定位游戏安装目录；主界面底部显示当前下载源。
- **开始游戏旁新增「游戏管理」菜单**：「检查更新」「添加桌面快捷方式」「打开游戏目录」「修复」「卸载」集中在一个菜单里，常用操作不用翻找；「检查更新」可以直接向下载源查询游戏新版本。
- **桌面快捷方式可直接开始游戏**：通过「添加桌面快捷方式」创建的快捷方式，双击后直接进入游戏，启动校验与运行状态提示仍由启动器完成；Linux 上会生成对应的 `.desktop` 入口。
- **记忆窗口位置和大小**：开启后，下次启动会恢复上次的窗口位置与尺寸。
- **设置分区重新整理**：设置按「常规、外观、游戏、下载与网络、高级、关于」重新归类——更新通道移入「常规 → 更新」，状态面板与远程内容开关移入「常规 → 显示」，兼容运行环境设置归入「游戏」分区（Linux）。
- **界面做减法**：启动时不再弹出「上次会话异常结束」的恢复对话框，异常仍会记入日志，可随时从设置导出反馈；底部状态面板只保留「不显示」与「简略」两种显示，不再提供「详细」模式。
- **状态和失败提示更有用**：公告等远程内容加载失败时可就地「重试」。提示条在鼠标悬停时暂停消失，网络异常会给出更易理解的原因和处理方向。

#### 网络、诊断与平台支持

- **代理连接更可靠**：支持 Windows 系统代理的自动配置脚本及需认证的代理；Linux 的「系统」代理可读取 GNOME 设置。使用 Clash、mihomo 等 Fake-IP 模式时，启动器会识别常见的虚拟地址范围，并在连接仍失败时给出针对性建议。HTTP/2 默认启用，也可在设置中关闭。常见的 SOCKS 代理地址写法也能正确识别；请求被重定向到其他站点时会自动剥离授权信息。
- **诊断信息更方便反馈**：导出日志时可指定时间范围与内容；发生无法恢复的错误时会显示崩溃编号、技术细节和打开日志目录的入口。Linux 诊断还会记录运行器输出、进程、图形环境和兼容环境信息。日志条目以简短的模块标签作为标题，更易翻阅；导出的日志会隐去网址中的查询参数（含玩家 UID），崩溃报告也不会包含用户目录路径。
- **首次发布 macOS 与 Linux 构建**：`v1.0.0` 只有 Windows 安装程序与免安装压缩包；本版新增 macOS arm64 实验构建，并为 Linux x64 提供压缩包、AppImage、`.deb`、`.rpm` 与面向 Arch 用户的 PKGBUILD 打包配方，各安装格式的桌面入口提供简体中文、繁体中文与日文名称与描述。Linux 兼容运行环境可在设置中选择（自动、UMU/Proton 或 Wine），实时显示各环境的可用状态，也可自定义运行器、Prefix 与 Proton 路径。Linux 启动前会检查兼容环境的常见路径、权限和文件系统问题，并给出修正建议。

### 技术更改

以下记录实现层面的主要变化，供需要核对发布内容的读者展开查看。

<details><summary>展开查看技术细节</summary>

- Windows 自更新由独立 helper 在主进程退出后执行；`SHA256SUMS`、版本元数据、release tag 与包名必须互相对应。便携版仅在新版本成功启动后才删除旧目录备份，启动失败时尝试恢复；自更新状态机已从壳层抽离为独立协作方。
- 游戏文件写入边界会再次检查运行中进程；下载采用临时文件、断点续传与 CRC64 校验。卸载的目录遍历可处理链接、只读文件与无法删除的条目，而不会让整个结果静默失败。
- 跨平台兼容运行环境接入 UMU/Proton 与 Wine；Linux 运行中识别结合启动器所有权标记与安装目录映射。单实例唤起在 Unix 上使用数据根内的本地套接字。Windows 安装程序由 NSIS 迁移到 Inno Setup 7，随附简体中文与日文语言文件，并保留对旧 NSIS 安装的一次性升级桥接；桌面快捷方式经游戏目录内的启动脚本或 `--launch-game` 参数直启游戏。安装器资产按平台归入 `windows/` 子目录，Linux 打包启动资产统一为共用模板并新增 Arch 系配方与 RPM 路径。
- 资源面板按 MD3 结构级重设计（ADR-041）；首次设置确认页改为完成徽章加图标摘要行；路径展示改为段感知中间省略，并拆出字段输入基类，等宽只保留给 UID 输入。
- 发布说明预览支持标题、列表、提示块与任务列表；链接和图片目标会经过限制。壁纸与横幅按窗口实际尺寸受限解码。设置、下载状态等持久化写入继续保持原子替换与统一的保存入口。
- 依赖升级：`xunit.v3` 至 4.0.1、`Microsoft.NET.Test.Sdk` 至 18.10.1，并保留既有测试路径；分发流程继续附 SHA-256 摘要与构建来源证明。

</details>
