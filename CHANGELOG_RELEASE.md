## v1.1.0-beta.6

> [!NOTE]
> 本版本集中修复主题色同步、超大自定义背景图性能与日志查看器布局问题：Windows 系统强调色变化可即时反映到“跟随系统”主题色；背景图按窗口所需尺寸解码，壁纸取色移出 UI 线程；日志查看器则消除了重复留白并在筛选结果变化时保持稳定高度。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.5`，这些向后兼容的缺陷修复将预发布号递增为 `v1.1.0-beta.6`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

### 主题与配色

- **即时同步 Windows 系统主题色**：当主题色来源设为“跟随系统”时，运行中的启动器会监听系统颜色变化并立即重建 M3 配色，同时更新设置页的主题色预览，无需重启或重新切换设置。

- **进度条遵循程序主题设置**：主操作进度条与加载状态不再直接继承 Windows 系统强调色，统一使用程序当前的 `Launcher.Color.Primary` 主题令牌；Toast 的成功、警告、错误等语义色保持不变。

### 背景图性能

- **大尺寸自定义背景图不再持续拖慢界面**：文件、文件夹随机壁纸与远端缓存图统一按窗口物理像素和铺满比例受限解码，避免 8K 等超大位图在每个渲染帧重复缩采样；窗口显著放大或 DPI 提升时会静默刷新同一张已选图片。

- **壁纸取色不再阻塞 UI 线程**：降采样与主题色量化改为后台执行，并使用代数守卫防止快速换图时旧结果覆盖新色板；保存设置前会等待当前壁纸的取色结果。

### 日志查看器

- **修复内容区重复留白**：移除日志列表、空状态与“加载更早日志”在共享对话框正文内边距之外重复叠加的外边距，使内容区恢复标准密度。

- **筛选条与正文对齐**：筛选按钮和搜索框的左右边距改为与 Panel 正文的 24px 水平内边距一致。

- **筛选结果切换时保持稳定高度**：日志查看器固定使用既有高度令牌；无匹配日志与存在运行日志时保持相同高度，列表内容继续在对话框内部虚拟化和滚动。
