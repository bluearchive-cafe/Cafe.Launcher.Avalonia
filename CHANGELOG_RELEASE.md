## v1.1.0-beta.7

![Cafe Launcher v1.1.0-beta.7 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.7/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.7-release-banner.png)

> [!NOTE]
> 本版本聚焦语言一致性与安装、更新链路的可靠性打磨：启动后界面语言不再与所选语言混杂显示；安装向导在选择路径阶段即时探测可写性，受保护目录提前给出指引；更新检查在只读游戏目录下不再误报失败；Cafe 下载源故障转移修复；四语言文案按地区习惯统一表述。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.6`，这些向后兼容的缺陷修复与体验改进将预发布号递增为 `v1.1.0-beta.7`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

### 语言与本地化

- **修复启动后界面语言混杂**：启动序列可能在后台线程应用语言设置，此前设置面板、关于页法务信息等目录绑定会按系统文化回退解析（如简体中文系统上与所选语言混杂显示），保存设置后才恢复；现在所有文案查找均按所选语言显式解析，启动后立即与所选语言一致。

- **日语文案与下载、校验提示可读性**：日语更新提示导航词与设置页实际标签对齐；本地清单缺失提示去除内部字段名，改为用户视角描述；启动校验描述补充本地与远程清单的快慢、可靠性取舍说明；下载源描述标明 Cafe 源由蔚蓝咖啡厅（bluearchive.cafe）日服汉化组提供；统一省略号与中日语句尾标点。

- **「本地化资源」按地区习惯表述**：简体中文作「本地化（汉化）资源」、繁体中文作「中文化資源」、英文作「Chinese-localized resources」、日文作「中国語化リソース」，各语言不再共用直译。

### 下载与游戏管理

- **Cafe 下载源故障转移修复**：Cafe 源主备 CDN 共用同一地址——镜像为单宿主，官方备用路径在该主机上并不存在，此前故障转移到备线路必然失败；现在备线路与主线路一致，切换后可正常继续下载（文件校验不受影响）。

- **只读游戏目录下更新检查不再误报失败**：差异为零的「已是最新」路径此前会无条件重写安装状态文件，在 Program Files 等非提权不可写位置每次检查更新都抛出异常并误报失败；现在状态与待提交内容完全一致时直接跳过重写，确需提交时先探测目录可写性，不可写时给出本地化的失败提示。

### 安装向导

- **选路径即时探测可写性**：全新安装与修复损坏安装需要写权限，现在选择路径阶段即探测目标目录（不存在时向上探测最近已存在祖先），Program Files 等受保护位置在向导内即被拦下，并展示锁图标与「以管理员身份运行或更换目录」指引（四语言）；已有效的安装只读即可运行，不做探测。

- **路径输入不再逐字符卡顿**：路径校验按 300ms 防抖合并击键，慢速介质下不再每个字符触发全量状态读取；可写性探测移出 UI 线程，UNC 网络路径的探测延迟不再冻结界面；快速输入时不再出现虚假「不可访问」状态闪烁。

### 设置与主题

- **更新通道设置移至「高级」分类**：更新通道从「下载与网络」的连接组移至「高级」页新建的「更新」组，与下载连接类设置分离，两个分类的描述文案同步调整。

- **动态中性色层级修正**：修正动态配色下中性色层级与外观设置相关文案。

### 工程质量

- **依赖升级**：Avalonia 升级至 12.1.2、Microsoft.Extensions.DependencyInjection 升级至 10.0.11，第三方声明同步再生。

- **安全与清理**：写探测改用随机探针文件名，消除固定文件名被符号链接利用的截断面；移除 40 个零引用本地化键与遗留设计令牌、图标资源。

- **测试加固**：补齐零差异匹配跳过与只读目录闸口的端到端行为测试；中性默认表守卫测试改为钉住 `App.axaml` 声明值，防止令牌改色静默漂移。
