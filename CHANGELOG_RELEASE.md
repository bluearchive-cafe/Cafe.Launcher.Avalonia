## v1.1.0-beta.1

![Cafe Launcher v1.1.0-beta.1 更新概览](https://github.com/bluearchive-cafe/Cafe.Launcher.Avalonia_Release/releases/download/v1.1.0-beta.1/cafe-launcher-v1.1.0-beta.1-release-banner.png)

> [!NOTE]
> 本版本完成了启动器视觉系统的整体升级，并新增游戏快捷操作、窗口状态记忆和更完整的动态配色能力。发布流程也扩展为 Windows、macOS 与 Linux 三平台产物。
>
> 由于这些变化属于向后兼容的功能扩展而非补丁修复，版本按 SemVer 从 `1.0.1-beta.1` 提升至 `1.1.0-beta.1`；当前仍为 Beta 预发布版本。
>
> macOS 与 Linux 版本目前仅作为实验性构建提供，尚未完成针对性适配与充分测试，可能存在兼容性或功能异常，暂不保证运行效果。

### 界面与交互

- **Fluent / Material 3 视觉系统重构**：将颜色、排版、间距、圆角、阴影、状态层和动效统一迁移到语义化设计令牌；主窗口、设置、对话框、安装向导、诊断、Toast 与远端内容区域采用一致的表面层级和控件状态。

- **统一对话框框架**：设置页、日志查看器、调试面板、安装向导和确认对话框统一使用新的 `DialogSurface`；规范标题、内容、工具栏、页脚和状态样式，并改进模态隔离、焦点恢复与 Escape 关闭行为。

- **设置工作区重构**：分类导航加入语义图标和明确的选中状态，设置行与下拉选择器改为可复用控件；优化键盘导航、滚动区域和最小点击高度，保存/取消操作固定在内容区底部，在长内容滚动时仍保持可见。

- **动效系统完善**：新增启动器入场、底部任务面板状态变形、壁纸交叉淡化、向导步骤切换、Toast 堆栈和横幅方向切换动效；“减少动态效果”会跳过或立即结束这些动画，并暂停自动轮播。

- **通知交互增强**：操作失败可通过 Toast 直接重试或打开日志；带操作的通知不会自动消失，执行期间会显示明确的禁用/处理中状态，多个通知按最新优先排列并独立退出。

- **首启向导改版**：改为紧凑的单表面分步流程，补充游戏目录即时状态、复核页快速返回编辑、退出确认以及步骤滚动复位；首启时会正确应用系统动效偏好。

### 游戏与个性化

- **新增游戏快捷操作**：主界面的游戏操作菜单现在可以主动检查游戏更新、打开游戏目录，并在 Windows 桌面创建指向已安装游戏的快捷方式；各类失败会给出对应提示。

- **Material 3 动态配色**：主题色由完整的 M3 色彩角色驱动，支持 Tonal Spot、Vibrant、Expressive、Fidelity、Content、Monochrome、Neutral 和 Rainbow 八种配色变体，并可选择品牌蓝或跟随种子色的中性色策略。

- **壁纸取色选项**：新增 Celebi + Score、Wu、Wsmeans 与旧版 Octree 四种取色算法；切换取色算法、配色变体、中性色策略或明暗主题时会即时刷新预览和界面颜色。

- **记住窗口状态**：设置中新增“记住窗口位置和大小”，启用后会保存正常窗口状态下的位置与尺寸，并在下次启动时恢复。

### 可靠性与质量

- **原子化状态写入**：设置、公告已读状态和下载检查点统一改为临时文件替换写入，降低程序中断时留下不完整 JSON 文件的风险。

- **操作反馈收敛**：启动器初始化、状态刷新、安装/更新和修复错误改为面向具体操作的通知；游戏状态刷新严格按订阅顺序完成，避免界面使用尚未同步的快照。

- **可访问性与视觉回归**：扩充键盘焦点、自动化名称、对比度、最小交互尺寸和模态隔离契约测试；新增主界面、进度面板、设置页、确认对话框与 Toast 的 Headless 黄金截图回归。

### 分发与工程

- **三平台分发**：发布脚本现在可生成 Windows `win-x64` ZIP、macOS arm64 `.app` ZIP、Linux x64 `tar.gz` 与 AppImage，并加入各平台应用图标和启动元数据。

- **Windows 安装器迁移**：安装器由 NSIS 迁移到 Inno Setup 6.3+；升级时会校验并安全移除可识别的旧 NSIS 安装，卸载时可选择是否删除本地应用数据。

- **发布流水线拆分**：CI 分离跨平台打包、Windows 安装器测试和 GitHub Release 发布阶段，加入依赖缓存、并发取消、超时和短期产物传递，并升级所用 GitHub Actions 版本。

- **设计与测试工具**：Debug 构建新增设计令牌画廊；补充动态配色、可访问性、安装器、快捷方式、原子文件写入和 UI 样式契约测试。
