## v1.1.0-beta.2

![Cafe Launcher v1.1.0-beta.2 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.2/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.2-release-banner.png)

> [!NOTE]
> 本版本重点改善 Windows 安装与升级体验，并修复游戏文件落盘、壁纸切换和设置界面的多项问题；同时补充隐私政策、第三方许可与默认壁纸版权信息。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.1`，这些向后兼容的新增与修复将预发布号递增为 `v1.1.0-beta.2`，不改变计划中的稳定版主次版本号。
>
> macOS 与 Linux 版本目前仍为实验性构建，尚未完成针对性适配与充分测试，可能存在兼容性或功能异常，暂不保证运行效果。

### Windows 安装与升级

- **现代目录选择器**：安装目录的“浏览”按钮改用 Windows 现代文件夹对话框，支持地址栏、导航和搜索；对话框不可用时会自动回退到内置选择器。

- **自动沿用原安装目录**：升级时优先读取现有 Inno Setup 或受信任的旧版 NSIS 安装路径，不再默认跳回新的安装目录，也不会因目标目录已存在而重复确认。

- **更安全的旧版迁移**：只有在用户确认开始安装后才移除旧版 NSIS 安装；取消向导不会影响当前版本，并继续校验卸载器路径以避免执行异常注册项。

- **修复安装器兼容问题**：修正卸载数据检查在安装阶段触发的错误，并将发布流水线升级至 Inno Setup 7；仓库仍保留本地使用 Inno Setup 6.3+ 编译所需的语言文件。

### 游戏安装与可靠性

- **更稳健地覆盖游戏文件**：安装、更新、修复或清理文件前会先移除目标文件的只读属性，避免同一文件反复触发 `UnauthorizedAccessException`。

- **提前检查目录写权限**：下载开始前即验证游戏目录是否可写；权限不足时会立即停止并显示具体路径和明确提示，避免下载大量数据后才失败。

- **修复壁纸切换崩溃**：壁纸交叉淡化期间，旧位图会在视图确认解除引用后再释放，避免窗口最小化、遮挡或卡顿时因渲染已释放位图而崩溃；横幅替换与退出清理采用相同的安全顺序。

### 界面与内容

- **设置项按场景显示**：仅当主题色来源设为壁纸时显示取色算法选项，避免在品牌色或自定义颜色模式下出现无效设置。

- **修正多行设置文案布局**：提示信息改为设置行内渲染，并调整多行内容的上下间距，使分隔线和控件排列更整齐。

- **更新默认壁纸**：采用画师 めるき（Meruki）的插画《初めてのゲーム》作为新默认壁纸，并在“关于”页面及项目文档中注明作者与作品来源。

- **补充隐私与版权入口**：在“关于”页面加入隐私政策和默认壁纸原作链接，方便直接查看相关说明。

### Linux 分发

- **修复 AppImage 启动入口**：补齐标准 AppDir 根目录所需的可执行 `AppRun`，并在打包完成后实际执行 runtime、`AppRun` 和 .NET apphost 启动链路，避免生成成功但运行时立即退出。

- **使用可写的默认游戏目录**：AppImage 运行时不再把默认游戏目录解析到只读的临时挂载点，而是使用用户主目录下的 `YostarGames/BlueArchive_JP`。

- **固定并验证打包工具链**：CI 固定 `appimagetool` 与 Type 2 runtime 的版本和 SHA-256，禁止构建时隐式下载可变的最新 runtime；同时在 Ubuntu 24.04 上通过 Xvfb 执行完整桌面启动烟测。

### 合规与工程维护

- **完善许可信息**：新增根据实际 NuGet 依赖生成的第三方许可清单，并统一应用、安装器和程序文件中的产品元数据。

- **精简内置资源**：移除版本号连点触发的音频彩蛋及不再使用的 NAudio 依赖；12 月 8 日日期彩蛋保持不变。

- **改进发布工具**：统一 PowerShell 脚本的 UTF-8 控制台输出与 LF 行尾，升级发布产物下载 Action，并补充安装器、下载权限、壁纸生命周期、设置布局和本地化契约测试。
