## v1.1.0-beta.6

![Cafe Launcher v1.1.0-beta.6 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.6/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.6-release-banner.png)

> [!NOTE]
> 本版本围绕主题配色、大图性能与日志查看器进行打磨：Windows 系统强调色变化可即时同步“跟随系统”的主题色；壁纸与运营横幅等大图按窗口所需尺寸解码并限制驻留大小，自定义壁纸在原路径覆盖内容后可正常刷新；日志查看器消除了重复留白并在筛选结果变化时保持稳定高度；同时包含下载校验提速与多项工程质量加固。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.5`，这些向后兼容的缺陷修复与性能改进将预发布号递增为 `v1.1.0-beta.6`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

> [!WARNING]
> 当前版本稳定性尚未完全验证，日常使用中可能出现崩溃或异常行为，请酌情更新；如遇问题，欢迎附上诊断日志反馈。

### 主题与配色

- **即时同步 Windows 系统主题色**：当主题色来源设为“跟随系统”时，运行中的启动器会监听系统颜色变化并立即重建 M3 配色，同时更新设置页的主题色预览，无需重启或重新切换设置。

- **进度条遵循程序主题设置**：主操作进度条与加载状态不再直接继承 Windows 系统强调色，统一使用程序当前的 `Launcher.Color.Primary` 主题令牌；Toast 的成功、警告、错误等语义色保持不变。

### 背景图与横幅

- **大尺寸自定义背景图不再持续拖慢界面**：文件、文件夹随机壁纸与远端缓存图统一按窗口物理像素和铺满比例受限解码，避免 8K 等超大位图在每个渲染帧重复缩采样；窗口显著放大或 DPI 提升时会静默刷新同一张已选图片，且重解码按维度增长触发，避免反复抖动。

- **壁纸取色不再阻塞 UI 线程**：降采样与主题色量化改为后台执行，并使用代数守卫防止快速换图时旧结果覆盖新色板；保存设置前会等待当前壁纸的取色结果，取色失败会写入诊断日志。

- **自定义壁纸原路径替换内容后正常刷新**：文件来源壁纸的“来源未变”跳过守卫并入内容指纹（文件长度与最后写入时间），在原路径覆盖图片文件后立即换用新图，不再停留旧图；目录随机壁纸仍每次刷新。

- **运营横幅解码加入驻留尺寸上限**：新增横幅解码器，常规横幅按原尺寸解码、超过上限的图等比缩小驻留，超大运营横幅不再驻留上百 MB 内存；小于上限的横幅不会被放大，保持全宽显示策略。

### 日志查看器

- **修复内容区重复留白**：移除日志列表、空状态与“加载更早日志”在共享对话框正文内边距之外重复叠加的外边距，使内容区恢复标准密度。

- **筛选条与正文对齐**：筛选按钮和搜索框的左右边距改为与 Panel 正文的 24px 水平内边距一致。

- **筛选结果切换时保持稳定高度**：日志查看器固定使用既有高度令牌；无匹配日志与存在运行日志时保持相同高度，列表内容继续在对话框内部虚拟化和滚动。

### 性能与工程质量

- **下载与安装验证更省时**：消除安装验证阶段的重复 CRC64 整读哈希，并以 slicing-by-8 查表提升单次哈希吞吐；下载服务拆分准备阶段与下载校验循环，便于维护。

- **架构与可维护性整理**：模态对话框契约迁至共享 ViewModels 层并明确 Shell 壳层定位；零散的调试输出统一收敛进本地诊断服务并补齐核心服务 XML 文档；异步方法统一 `Async` 后缀；删除已被跨进程启动桥取代的死入口。

- **本地化键守卫接入 CI**：禁止裸本地化 key 字符串的源码守卫接入 `verify.ps1` 与 CI，并覆盖 `I18n` 索引器形态，拼写错误在构建期即被拦截。

- **构建与供应链加固**：全部 GitHub Actions 固定到 commit SHA，Dependabot 接入 nuget 生态；CI 显式接线 locked mode 还原与确定性构建，缓存键覆盖中央包版本与 lock 文件；修复 coverage 脚本在 PowerShell 5.1 下的编码解析与 coverlet 插桩文件锁竞争问题。
