## v1.1.0-beta.5

![Cafe Launcher v1.1.0-beta.5 更新概览](https://raw.githubusercontent.com/bluearchive-cafe/Cafe.Launcher.Avalonia/v1.1.0-beta.5/docs/assets/release-banners/cafe-launcher-v1.1.0-beta.5-release-banner.png)

> [!NOTE]
> 本版本以稳定性加固为主线：修复所有平台因文件选择器依赖注入配置错误导致的启动崩溃，以及 Linux 分发包的跨进程信号启动崩溃；修复运行环境选择与进程跟踪回归，恢复启动失败诊断的完整上下文；并对远端内容链接引入协议白名单、重构依赖管理与多项内部架构以提升性能与可维护性。
>
> 当前版本仍属于 `1.1.0` 的 Beta 预发布序列。相较 `v1.1.0-beta.4`，这些向后兼容的新增与修复将预发布号递增为 `v1.1.0-beta.5`，不改变计划中的稳定版主次版本号。
>
> Windows 为正式支持平台；macOS 与 Linux 仍为实验性构建。Linux 兼容运行环境的反作弊兼容性仍受发行版、显卡驱动、Proton 版本组合影响，相关组件大版本更新后建议重新验证启动链路。

### 关键修复

- **修复所有平台打开应用即崩溃**：文件选择器服务重构后，生产入口按具体类型解析服务，而依赖注入容器仅登记了接口映射，导致 Windows、macOS 与 Linux 均在创建主窗口前抛出 `NoServiceRegistered`。现让接口与具体类型共享同一个单例，并加入容器解析回归测试。

- **修复 Linux 分发包启动即崩溃**：`--launch-game` 跨进程信号此前在 Unix 上无条件使用命名事件（.NET 不支持，构造即抛出异常），导致 Linux 分发包一启动就崩溃。现改用用户数据目录下的 Unix 域套接字转发信号，Windows 行为保持不变。

- **修复运行环境选择与进程跟踪回归**：修复自动模式下运行环境选择与游戏进程跟踪在上一轮重构中引入的回归。

- **恢复启动失败诊断的完整上下文**：启动失败时的诊断信息重新包含所选运行器（手动选中或 Auto）、完整请求上下文与候选运行器的可用性证据，便于准确定位故障层级。

### 安全与远端内容

- **远端链接协议白名单**：横幅、新闻、公告与社媒映射点的远端 URL 现经过协议白名单校验，非白名单协议的链接不再进入打开与下载链路。

### 界面改进

- **关于页区分源码与发布仓库链接**：关于分区新增源码仓库链接，与发布仓库区分展示；修正官方网站链接指向。

- **新增 `--show-hidden-settings` 调试参数**：启动时附带该参数可显示默认隐藏的设置项（如主题色提取算法、自定义取色设置），仅影响当前进程、不持久化。

- **UI 修复与细节**：移除横幅悬停时的白色遮罩；统一运行时路径输入框尺寸；更新部分文案并修正资源契约。

### 性能与工程质量

- **UI 线程响应优化**：移除 `async void` 与 `unsafe` 代码，异步上下文中的日志与错误处理全部走异步路径；背景图等大图解码移入后台线程，避免解码卡帧。

- **网络层优化**：按代理模式复用代理 HTTP handler，配置变更时才重建，消除每次请求租约新建 handler 的开销。

- **依赖管理收紧**：启用中央包版本管理（`Directory.Packages.props`）与依赖锁定文件；开启完整 NuGet 依赖漏洞审计，已知漏洞将直接阻断构建。经扫描确认当前依赖链无已知漏洞。

- **架构与可维护性**：启动编排、游戏运行环境、下载会话等核心模块完成收敛重构；本地化键全部常量化（拼写错误从运行时降级为编译期错误）；诊断功能与游戏操作之间的横向依赖解耦；补齐大量单元与无头 UI 回归测试。
