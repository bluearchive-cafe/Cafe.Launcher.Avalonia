<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Cafe.Launcher.Avalonia.ViewModels"
        xmlns:models="using:Cafe.Launcher.Avalonia.Models"
        xmlns:services="using:Cafe.Launcher.Avalonia.Services"
        xmlns:views="using:Cafe.Launcher.Avalonia.Views"
        xmlns:materialIcons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1300" d:DesignHeight="754"
        x:Class="Cafe.Launcher.Avalonia.Views.MainWindow"
        x:Name="Root"
        x:DataType="vm:MainWindowViewModel"
        Icon="/Assets/app-icon.ico"
        Title="Cafe Launcher"
        Width="1300"
        Height="754"
        MinWidth="1024"
        MinHeight="640"
        CanResize="False"
        WindowDecorations="BorderOnly"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaTitleBarHeightHint="0"
        Background="{DynamicResource LauncherMediaPlaceholderBrush}">


    <Window.Styles>
        <StyleInclude Source="avares://Cafe.Launcher.Avalonia/Views/MainWindow.Styles.axaml"/>
    </Window.Styles>

    <Grid>
        <Rectangle Fill="{Binding Background.BackgroundFillBrush}"/>
        <Image Source="{Binding Background.BackgroundImageSource}" Stretch="{Binding Background.BackgroundStretch}"/>

        <Grid RowDefinitions="Auto,*">
            <Grid ColumnDefinitions="*,Auto" Height="48">
                <Grid.Background>
                    <StaticResource ResourceKey="LauncherTitleBarGradient"/>
                </Grid.Background>
                <!-- W3: Right-click context menu on title bar -->
                <Grid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="{Binding Shell.I18n.ShowLauncher}" Command="{Binding WindowChrome.ExecuteRestoreWindowCommand}"/>
                        <MenuItem Header="{Binding Shell.I18n.Settings}" Command="{Binding WindowChrome.ShowSettingsCommand}"/>
                        <Separator/>
                        <MenuItem Header="{Binding Shell.I18n.Close}" Command="{Binding WindowChrome.CloseCommand}"/>
                    </ContextMenu>
                </Grid.ContextMenu>
                <StackPanel Classes="titlebar-brand-row">
                    <TextBlock Text="{Binding Shell.ProductName}" Classes="titlebar-brand"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Classes="titlebar-actions">
                    <Button Classes="chrome" Command="{Binding ResourcePanel.OpenResourcePanelCommand}" ToolTip.Tip="{Binding Shell.I18n.ResourcePanel}">
                        <materialIcons:MaterialIcon Kind="Web" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}" Foreground="{DynamicResource LauncherOnChromeBrush}"/>
                    </Button>
                    <Button Classes="chrome settings" Command="{Binding WindowChrome.ShowSettingsCommand}" ToolTip.Tip="{Binding Shell.I18n.Settings}">
                        <materialIcons:MaterialIcon Kind="CogOutline" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}" Foreground="{DynamicResource LauncherOnChromeBrush}"/>
                    </Button>
                    <Button Classes="chrome" Command="{Binding WindowChrome.MinimizeCommand}" ToolTip.Tip="{Binding Shell.I18n.Minimize}">
                        <materialIcons:MaterialIcon Kind="Minus" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}" Foreground="{DynamicResource LauncherOnChromeBrush}"/>
                    </Button>
                    <Button Classes="chrome close" Command="{Binding WindowChrome.CloseCommand}" ToolTip.Tip="{Binding Shell.I18n.Close}">
                        <materialIcons:MaterialIcon Kind="Close" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}" Foreground="{DynamicResource LauncherOnChromeBrush}"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Border Classes="surface remote-surface"
                        IsVisible="{Binding RemoteContent.HasRemoteContent}">
                    <ScrollViewer Classes="scroll-pad">
                        <StackPanel Spacing="{StaticResource LauncherSpacingMd}">
                            <Border IsVisible="{Binding RemoteContent.HasNotice}" Classes="notice-card">
                                <StackPanel Spacing="{StaticResource LauncherSpacingXs}">
                                    <TextBlock Text="{Binding Shell.I18n.Notice}" Classes="panel-title"/>
                                    <ScrollViewer MaxHeight="110" Classes="scroll-pad">
                                        <TextBlock Text="{Binding RemoteContent.NoticeText}" Classes="body" TextWrapping="Wrap"/>
                                    </ScrollViewer>
                                </StackPanel>
                            </Border>

                            <StackPanel Spacing="{StaticResource LauncherSpacingSm}" IsVisible="{Binding RemoteContent.HasBannerItems}">
                                <!-- L5/A3: Banner header with title, page indicator, and pause button -->
                                <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="{StaticResource LauncherSpacingSm}">
                                    <TextBlock Text="{Binding Shell.I18n.Banners}" Classes="panel-title" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding RemoteContent.CarouselPageText}" Classes="caption" VerticalAlignment="Center"/>
                                    <Button Grid.Column="3" Classes="icon-button compact" Cursor="Hand"
                                            Command="{Binding RemoteContent.ToggleCarouselLoopCommand}"
                                            ToolTip.Tip="{Binding RemoteContent.CarouselPauseTooltip}">
                                        <materialIcons:MaterialIcon Kind="{Binding RemoteContent.CarouselPauseIcon}" Width="{StaticResource LauncherIconSm}" Height="{StaticResource LauncherIconSm}"
                                                                    Foreground="{DynamicResource LauncherTextSecondaryBrush}"/>
                                    </Button>
                                </Grid>
                                <!-- Banner Carousel with crossfade transition -->
                                <Carousel x:Name="BannerCarousel"
                                          ItemsSource="{Binding RemoteContent.BannerItems}"
                                          SelectedIndex="{Binding RemoteContent.CarouselSelectedIndex, Mode=TwoWay}"
                                          Height="220"
                                          CornerRadius="{StaticResource LauncherRadiusSm}" ClipToBounds="True">
                                    <Carousel.Styles>
                                        <Style Selector="ContentControl">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                        </Style>
                                    </Carousel.Styles>
                                    <Carousel.PageTransition>
                                        <CrossFade Duration="0:0:0.35"/>
                                    </Carousel.PageTransition>
                                    <Carousel.ItemTemplate>
                                        <DataTemplate x:DataType="models:RemoteContentItem">
                                            <Button Classes="banner-link"
                                                    Cursor="Hand"
                                                    Width="{Binding #BannerCarousel.Bounds.Width}"
                                                    Height="{Binding #BannerCarousel.Bounds.Height}"
                                                    Command="{Binding DataContext.RemoteContent.OpenExternalUrlCommand, ElementName=Root}"
                                                    CommandParameter="{Binding Url}">
                                                <Panel>
                                                    <!-- C3: Placeholder with text when banner fails to load -->
                                                    <Border Classes="media-placeholder">
                                                        <TextBlock Text="{Binding DataContext.Shell.I18n.BannerLoadingFailed, ElementName=Root}"
                                                                   Classes="media-placeholder-text"
                                                                   IsVisible="{Binding BannerBitmap, Converter={x:Static ObjectConverters.IsNull}}"/>
                                                    </Border>
                                                    <Image Source="{Binding BannerBitmap}"
                                                           Stretch="UniformToFill"/>
                                                </Panel>
                                            </Button>
                                        </DataTemplate>
                                    </Carousel.ItemTemplate>
                                </Carousel>
                                <!-- L5: Carousel indicator dots + left/right arrows -->
                                <Grid ColumnDefinitions="Auto,*,Auto">
                                    <Button Grid.Column="0" Classes="icon-button compact" Cursor="Hand"
                                            Command="{Binding RemoteContent.SelectPreviousBannerCommand}"
                                            IsVisible="{Binding RemoteContent.HasMultipleBanners}">
                                        <materialIcons:MaterialIcon Kind="ChevronLeft" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}"
                                                                    Foreground="{DynamicResource LauncherTextSecondaryBrush}"/>
                                    </Button>
                                    <ItemsControl Grid.Column="1" ItemsSource="{Binding RemoteContent.BannerDots}" HorizontalAlignment="Center">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource LauncherSpacingSm}"/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="models:BannerDot">
                                                <Button Classes="icon-button dot"
                                                        Cursor="Hand"
                                                        Command="{Binding DataContext.RemoteContent.SelectBannerCommand, ElementName=Root}"
                                                        CommandParameter="{Binding Index}">
                                                    <Border Classes="banner-dot"
                                                            Classes.active="{Binding IsActive}"/>
                                                </Button>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <Button Grid.Column="2" Classes="icon-button compact" Cursor="Hand"
                                            Command="{Binding RemoteContent.SelectNextBannerCommand}"
                                            IsVisible="{Binding RemoteContent.HasMultipleBanners}">
                                        <materialIcons:MaterialIcon Kind="ChevronRight" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}"
                                                                    Foreground="{DynamicResource LauncherTextSecondaryBrush}"/>
                                    </Button>
                                </Grid>
                            </StackPanel>

                            <StackPanel Spacing="{StaticResource LauncherSpacingSm}" IsVisible="{Binding RemoteContent.HasNewsItems}">
                                <TextBlock Text="{Binding Shell.I18n.News}" Classes="panel-title"/>
                                <!-- Category tabs -->
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource LauncherSpacingXs}">
                                        <ItemsControl ItemsSource="{Binding RemoteContent.NewsCategories}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource LauncherSpacingXs}"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="models:NewsCategory">
                                                    <Button Cursor="Hand"
                                                            Classes="news-tab"
                                                            Classes.active="{Binding IsActive}"
                                                            Command="{Binding DataContext.RemoteContent.SelectNewsCategoryCommand, ElementName=Root}"
                                                            CommandParameter="{Binding .}">
                                                        <TextBlock Text="{Binding Label}" VerticalAlignment="Center"/>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </ScrollViewer>
                                <!-- Filtered news items -->
                                <ItemsControl ItemsSource="{Binding RemoteContent.SelectedNewsCategory.Items}"
                                              IsVisible="{Binding RemoteContent.SelectedNewsCategory, Converter={x:Static ObjectConverters.IsNotNull}}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="models:RemoteContentItem">
                                            <Button Classes="content-link"
                                                    Command="{Binding DataContext.RemoteContent.OpenExternalUrlCommand, ElementName=Root}"
                                                    CommandParameter="{Binding Url}">
                                                <Border Classes="content-row">
                                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource LauncherSpacingLg}">
                                                        <TextBlock Text="{Binding Title}" Classes="value" TextTrimming="CharacterEllipsis"/>
                                                        <TextBlock Grid.Column="1" Text="{Binding Subtitle}" Width="80" TextAlignment="Right" TextTrimming="CharacterEllipsis" Classes="caption"/>
                                                    </Grid>
                                                </Border>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>

                            <StackPanel Spacing="{StaticResource LauncherSpacingSm}" IsVisible="{Binding RemoteContent.HasSocialMediaItems}">
                                <TextBlock Text="{Binding Shell.I18n.SocialMedia}" Classes="panel-title"/>
                                <ItemsControl ItemsSource="{Binding RemoteContent.SocialMediaItems}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="models:RemoteContentItem">
                                            <Button Classes="social-chip"
                                                    Command="{Binding DataContext.RemoteContent.OpenExternalUrlCommand, ElementName=Root}"
                                                    CommandParameter="{Binding Url}">
                                                <StackPanel Classes="button-content">
                                                    <materialIcons:MaterialIcon Kind="{Binding SocialIconKind}" Width="{StaticResource LauncherIconSm}" Height="{StaticResource LauncherIconSm}"
                                                                                Foreground="{DynamicResource LauncherTextSecondaryBrush}"/>
                                                    <TextBlock Text="{Binding Title}" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <Border IsVisible="{Binding Operations.IsInstallPanelVisible}"
                        Classes="bottom-panel">
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="*,Auto" ColumnSpacing="{StaticResource LauncherSpacingXl}">
                        <StackPanel VerticalAlignment="Center" Spacing="{StaticResource LauncherSpacingSm}">
                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource LauncherSpacingLg}">
                                <TextBlock Classes="caption" Text="{Binding Shell.VersionText}"/>
                                <TextBlock Classes="caption" Text="{Binding Shell.DiskSpaceText}"/>
                                <TextBlock Classes="caption" Text="{Binding Shell.NetworkText}"/>
                            </StackPanel>
                            <Border Classes="path-field">
                                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="{StaticResource LauncherSpacingSm}">
                                    <TextBlock Text="{Binding Shell.I18n.Path}" Classes="value" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Shell.PathText}" Classes="caption" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    <Button Grid.Column="2" Classes="icon-link" Command="{Binding Settings.ChooseGamePathCommand}" VerticalAlignment="Center"
                                            IsEnabled="{Binding Shell.IsBusy, Converter={x:Static BoolConverters.Not}}">
                                        <StackPanel Classes="button-content">
                                            <materialIcons:MaterialIcon Kind="FolderOpen" Width="{StaticResource LauncherIconSm}" Height="{StaticResource LauncherIconSm}"/>
                                            <TextBlock Text="{Binding Shell.I18n.ChangePath}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </Grid>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="{StaticResource LauncherSpacingMd}" VerticalAlignment="Center">
                            <Button Classes="flat-action bottom-action" Command="{Binding RefreshCommand}" ToolTip.Tip="{Binding Shell.I18n.RefreshTooltip}"
                                    IsEnabled="{Binding Shell.IsBusy, Converter={x:Static BoolConverters.Not}}">
                                <StackPanel Classes="button-content">
                                    <materialIcons:MaterialIcon Kind="Refresh" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}"/>
                                    <TextBlock Text="{Binding Shell.I18n.Refresh}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Classes="primary-action bottom-action" Command="{Binding Operations.InstallOrUpdateCommand}" ToolTip.Tip="{Binding Operations.InstallButtonText}"
                                    IsEnabled="{Binding Shell.IsBusy, Converter={x:Static BoolConverters.Not}}">
                                <StackPanel Classes="button-content">
                                    <materialIcons:MaterialIcon Kind="Download" Width="{StaticResource LauncherIconXl}" Height="{StaticResource LauncherIconXl}" Foreground="{DynamicResource LauncherOnAccentBrush}"/>
                                    <TextBlock Text="{Binding Operations.InstallButtonText}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border IsVisible="{Binding Operations.IsProgressPanelVisible}"
                        Classes="bottom-panel">
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource LauncherSpacingXl}" VerticalAlignment="Center">
                        <StackPanel Spacing="{StaticResource LauncherSpacingSm}" VerticalAlignment="Center">
                            <!-- Title + percentage -->
                            <Grid ColumnDefinitions="Auto,*,Auto">
                                <TextBlock Text="{Binding Operations.ProgressTitle}" Classes="progress-title" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="{Binding Operations.ProgressValue, StringFormat={}{0}%}"
                                           Classes="progress-title" VerticalAlignment="Center"
                                           Foreground="{DynamicResource LauncherAccentBrush}"/>
                            </Grid>
                            <!-- Progress bar -->
                            <ProgressBar Value="{Binding Operations.ProgressValue}" Minimum="0" Maximum="100" Height="8"/>
                            <!-- Stage | Speed | Size | ETA -->
                            <Grid ColumnDefinitions="Auto,*,Auto,Auto,Auto" ColumnSpacing="{StaticResource LauncherSpacingMd}">
                                <TextBlock Text="{Binding Operations.ProgressDetail}" Classes="caption"/>
                                <TextBlock Grid.Column="2" Text="{Binding Operations.ProgressSpeed}" Classes="caption"
                                           IsVisible="{Binding Operations.ProgressSpeed, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                <TextBlock Grid.Column="3" Text="{Binding Operations.ProgressSize}" Classes="caption"
                                           IsVisible="{Binding Operations.ProgressSize, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                                <TextBlock Grid.Column="4" Text="{Binding Operations.ProgressEstimated}" Classes="caption"
                                           Foreground="{DynamicResource LauncherAccentBrush}"
                                           IsVisible="{Binding Operations.ProgressEstimated, Converter={x:Static StringConverters.IsNotNullOrEmpty}}"/>
                            </Grid>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="{StaticResource LauncherSpacingSm}" VerticalAlignment="Center">
                            <Button Classes="flat-action bottom-action" Command="{Binding Operations.PauseResumeCommand}" VerticalAlignment="Center"
                                    IsVisible="{Binding Operations.CanPauseOperation}">
                                <StackPanel Classes="button-content">
                                    <materialIcons:MaterialIcon Kind="{Binding Operations.PauseResumeIcon}" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}"/>
                                    <TextBlock Text="{Binding Operations.PauseResumeText}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Classes="flat-action bottom-action" Command="{Binding Operations.StopOperationCommand}" VerticalAlignment="Center">
                                <StackPanel Classes="button-content">
                                    <materialIcons:MaterialIcon Kind="Stop" Width="{StaticResource LauncherIconMd}" Height="{StaticResource LauncherIconMd}"/>
                                    <TextBlock Text="{Binding Shell.I18n.Stop}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border IsVisible="{Binding Operations.IsControlPanelVisible}"
                        Classes="control-panel">
                    <Grid ColumnDefinitions="*,Auto" RowDefinitions="*,Auto" ColumnSpacing="{StaticResource LauncherSpacingXxl}">
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="{StaticResource LauncherSpacingLg}" VerticalAlignment="Center">
                            <Button Classes="launcher-control site" Command="{Binding WindowChrome.OpenOfficialSiteCommand}" ToolTip.Tip="{Binding Shell.I18n.OfficialSite}">
                                <StackPanel Classes="button-content">
                                    <materialIcons:MaterialIcon Kind="Web" Width="{StaticResource LauncherIconXl}" Height="{StaticResource LauncherIconXl}"/>
                                    <TextBlock Text="{Binding Shell.I18n.OfficialSite}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Classes="launcher-control start" Command="{Binding Operations.StartGameCommand}" ToolTip.Tip="{Binding Shell.I18n.StartGame}"
                                    IsEnabled="{Binding Shell.IsBusy, Converter={x:Static BoolConverters.Not}}">
                                <StackPanel Classes="button-content">
                                    <materialIcons:MaterialIcon Kind="PlayCircle" Width="{StaticResource LauncherIconXxl}" Height="{StaticResource LauncherIconXxl}" Foreground="{DynamicResource LauncherOnAccentBrush}"/>
                                    <TextBlock Text="{Binding Shell.I18n.StartGame}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="{StaticResource LauncherSpacingLg}">
                            <TextBlock Text="{Binding Shell.VersionText}" Classes="on-dark-caption"/>
                            <TextBlock Text="{Binding Shell.LaunchCheckText}" Classes="on-dark-caption"/>
                            <TextBlock Text="{Binding Shell.NetworkText}" Classes="on-dark-caption"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <views:MainWindowSettingsOverlay/>
        <views:MainWindowDialogsOverlay/>
        <views:MainWindowToastOverlay/>
    </Grid>
</Window>
